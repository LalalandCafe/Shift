# SHIFT Formula Reference

Auto-generated by `scripts/extract-formulas.mjs` on 2026-08-31.
Every line below is a real calculation pulled straight from the source, with its file and line number.

Scanned: lib, app/api, components (70 files)

---

## `app/api/dashboard/route.js`

```js
// Umbral que separa critico de advertencia (0.15 = 15% abajo del target)
```
<sub>app/api/dashboard/route.js:3</sub>

```js
function addDays(iso, n) {
```
<sub>app/api/dashboard/route.js:6</sub>

```js
function blendedWtd(rows) {
```
<sub>app/api/dashboard/route.js:12</sub>

```js
rows.forEach((r) => { h += r.wtd.hours; s += r.wtd.sales; });
```
<sub>app/api/dashboard/route.js:14</sub>

```js
export async function GET(request) {
```
<sub>app/api/dashboard/route.js:18</sub>

```js
// Mismo dia de la semana anterior. Comparar un WTD parcial contra
```
<sub>app/api/dashboard/route.js:30</sub>

_Mismo dia de la semana anterior. Comparar un WTD parcial contra_

```js
// una semana completa daria un delta falso.
```
<sub>app/api/dashboard/route.js:31</sub>

```js
detail: `${r.day.hours}h · $${Math.round(r.day.sales).toLocaleString("en-US")}`,
```
<sub>app/api/dashboard/route.js:52</sub>

```js
// Rendimiento medido con WTD, no con el dia suelto, porque a media
```
<sub>app/api/dashboard/route.js:56</sub>

```js
const ratio = r.wtd.splh / target;
```
<sub>app/api/dashboard/route.js:60</sub>

```js
const pct = Math.round((1 - ratio) * 100);
```
<sub>app/api/dashboard/route.js:61</sub>

```js
if (ratio < 1 - CRITICAL_PCT) {
```
<sub>app/api/dashboard/route.js:62</sub>

```js
detail: `${pct}% below target · ${Math.abs(r.wtd.overUnder)} hrs ${r.wtd.overUnder < 0 ? "over" : "under"}`,
```
<sub>app/api/dashboard/route.js:67</sub>

_── TENDENCIA ── Cada entrada lleva horas y ventas de ambas semanas. Sin eso el cliente_

```js
// solo puede promediar SPLH por tienda, y el promedio de un ratio no es el
```
<sub>app/api/dashboard/route.js:91</sub>

_── TENDENCIA ── Cada entrada lleva horas y ventas de ambas semanas. Sin eso el cliente solo puede promediar SPLH por tienda, y el promedio de un ratio no es el_

```js
// ratio de la suma. Con estos cuatro campos el Dashboard calcula el blended
```
<sub>app/api/dashboard/route.js:92</sub>

```js
const delta = r.wtd.splh - p.wtd.splh;
```
<sub>app/api/dashboard/route.js:101</sub>

```js
.sort((a, b) => a.delta - b.delta);
```
<sub>app/api/dashboard/route.js:117</sub>

```js
blendedCurrent: Math.round(curBlended),
```
<sub>app/api/dashboard/route.js:136</sub>

```js
blendedPrior: priBlended !== null ? Math.round(priBlended) : null,
```
<sub>app/api/dashboard/route.js:137</sub>

```js
blendedDelta: priBlended !== null ? Math.round(curBlended - priBlended) : null,
```
<sub>app/api/dashboard/route.js:138</sub>

```js
improving: trends.filter((t) => t.delta > 0).slice(-5).reverse(),
```
<sub>app/api/dashboard/route.js:143</sub>

---

## `app/api/demo/day/route.js`

_GET /api/demo/day?storeCode=10002&date=2026-07-19&day=Sunday_

```js
// Lee labor+ventas ya guardados en Supabase para esa tienda/dia y devuelve
```
<sub>app/api/demo/day/route.js:6</sub>

_GET /api/demo/day?storeCode=10002&date=2026-07-19&day=Sunday Lee labor+ventas ya guardados en Supabase para esa tienda/dia y devuelve las metricas calculadas (mismo resultado que veria SHIFT)._

```js
export async function GET(request) {
```
<sub>app/api/demo/day/route.js:8</sub>

```js
// Labor guardado para ese dia/tienda
```
<sub>app/api/demo/day/route.js:25</sub>

```js
splh: Math.round(metrics.splh),
```
<sub>app/api/demo/day/route.js:58</sub>

```js
overUnder: Math.round(metrics.overUnder),
```
<sub>app/api/demo/day/route.js:59</sub>

---

## `app/api/demo/email/route.js`

_GET /api/demo/email?storeCode=10002&date=2026-07-19&day=Sunday Genera el correo HTML real (mismo formato de SHIFT) para la tienda/dia de la demo._

```js
export async function GET(request) {
```
<sub>app/api/demo/email/route.js:9</sub>

---

## `app/api/drive-thru/route.js`

_GET /api/drive-thru?days=30&storeCode=10008&view=hourly GET /api/drive-thru?storeCode=10008&view=distribution_

```js
// Thresholds come from metric_targets and are passed through untouched. This
```
<sub>app/api/drive-thru/route.js:11</sub>

```js
function daysAgoISO(n) {
```
<sub>app/api/drive-thru/route.js:18</sub>

_PostgREST caps a single response at 1000 rows by default. A 90-day window across even one store's hourly rows, or the daily summary across every_

```js
// store, can exceed that - the same truncation class that once inflated WTD
```
<sub>app/api/drive-thru/route.js:26</sub>

_PostgREST caps a single response at 1000 rows by default. A 90-day window across even one store's hourly rows, or the daily summary across every store, can exceed that - the same truncation class that once inflated WTD_

```js
// SPLH. Same pattern as lib/report.js's fetchAllRows: page with .range()
```
<sub>app/api/drive-thru/route.js:27</sub>

```js
async function fetchAllRows(buildQuery, label) {
```
<sub>app/api/drive-thru/route.js:31</sub>

```js
async function loadTarget() {
```
<sub>app/api/drive-thru/route.js:45</sub>

_green_value is optional on this table: it lets a metric name its own_

```js
// green line instead of accepting target * 0.85. Drive-thru window time
```
<sub>app/api/drive-thru/route.js:47</sub>

_green_value is optional on this table: it lets a metric name its own green line instead of accepting target * 0.85. Drive-thru window time_

```js
// sets it (2:00 while the target is 2:30), so it must be selected here or
```
<sub>app/api/drive-thru/route.js:48</sub>

```js
export async function GET(request) {
```
<sub>app/api/drive-thru/route.js:60</sub>

```js
cur.bandGreen += r.band_green || 0;
```
<sub>app/api/drive-thru/route.js:142</sub>

```js
cur.bandLightGreen += r.band_light_green || 0;
```
<sub>app/api/drive-thru/route.js:143</sub>

```js
cur.bandLightRed += r.band_light_red || 0;
```
<sub>app/api/drive-thru/route.js:144</sub>

```js
cur.bandRed += r.band_red || 0;
```
<sub>app/api/drive-thru/route.js:145</sub>

```js
cur.atTarget += r.green_cars || 0;
```
<sub>app/api/drive-thru/route.js:146</sub>

```js
cur.windowSum += (r.avg_window_time || 0) * (r.car_count || 0);
```
<sub>app/api/drive-thru/route.js:148</sub>

```js
cur.menuSum += (r.avg_menu_time || 0) * (r.car_count || 0);
```
<sub>app/api/drive-thru/route.js:149</sub>

```js
cur.greetSum += (r.avg_greet_time || 0) * (r.car_count || 0);
```
<sub>app/api/drive-thru/route.js:150</sub>

```js
avgWindowTime: s.cars ? Math.round(s.windowSum / s.cars) : null,
```
<sub>app/api/drive-thru/route.js:163</sub>

```js
avgMenuTime: s.cars ? Math.round(s.menuSum / s.cars) : null,
```
<sub>app/api/drive-thru/route.js:164</sub>

```js
avgGreetTime: s.cars ? Math.round(s.greetSum / s.cars) : null,
```
<sub>app/api/drive-thru/route.js:165</sub>

```js
pctGreen: s.cars ? +((100 * s.atTarget) / s.cars).toFixed(1) : null,
```
<sub>app/api/drive-thru/route.js:166</sub>

```js
pctRed: s.cars ? +((100 * s.overRed) / s.cars).toFixed(1) : null,
```
<sub>app/api/drive-thru/route.js:167</sub>

---

## `app/api/email/route.js`

```js
export async function GET(request) {
```
<sub>app/api/email/route.js:4</sub>

---

## `app/api/export/route.js`

```js
function addDays(iso, n) {
```
<sub>app/api/export/route.js:7</sub>

```js
export async function GET(request) {
```
<sub>app/api/export/route.js:13</sub>

```js
// El ultimo dia con data manda para los totales de semana (WTD acumulado)
```
<sub>app/api/export/route.js:50</sub>

---

## `app/api/forecast/hourly/route.js`

```js
// Endpoint aparte y no un campo mas en /api/forecast a proposito: la curva
```
<sub>app/api/forecast/hourly/route.js:3</sub>

_Endpoint aparte y no un campo mas en /api/forecast a proposito: la curva solo se pide cuando alguien abre un dia. El planeador carga igual de rapido para quien nunca la abre._

```js
export async function GET(request) {
```
<sub>app/api/forecast/hourly/route.js:6</sub>

---

## `app/api/forecast/route.js`

```js
export async function GET(request) {
```
<sub>app/api/forecast/route.js:4</sub>

```js
export async function POST(request) {
```
<sub>app/api/forecast/route.js:30</sub>

_Borra un dia especifico, o toda la semana si se pasa weekStart. Sin esto no habia forma de deshacer un plan capturado, solo corregirlo._

```js
export async function DELETE(request) {
```
<sub>app/api/forecast/route.js:69</sub>

---

## `app/api/hme/sync-store/route.js`

```js
export async function POST(request) {
```
<sub>app/api/hme/sync-store/route.js:18</sub>

---

## `app/api/kitchen-day/route.js`

```js
export async function GET(request) {
```
<sub>app/api/kitchen-day/route.js:3</sub>

---

## `app/api/kitchen-trend/route.js`

```js
export async function GET(request) {
```
<sub>app/api/kitchen-trend/route.js:3</sub>

---

## `app/api/kitchen-week/route.js`

```js
export async function GET(request) {
```
<sub>app/api/kitchen-week/route.js:3</sub>

---

## `app/api/report/route.js`

```js
export async function GET(request) {
```
<sub>app/api/report/route.js:3</sub>

---

## `app/api/store-trend/route.js`

```js
export async function GET(request) {
```
<sub>app/api/store-trend/route.js:4</sub>

---

## `app/api/stores/route.js`

```js
export async function GET() {
```
<sub>app/api/stores/route.js:5</sub>

```js
export async function PATCH(request) {
```
<sub>app/api/stores/route.js:16</sub>

```js
// Los targets deciden quien sale en verde. Solo admin, y desde el paso 3
```
<sub>app/api/stores/route.js:17</sub>

---

## `app/api/sync/tattle/route.js`

_app/api/sync/tattle/route.js_

```js
// Automates the manual Tattle Report: pull Google and Yelp reviews,
```
<sub>app/api/sync/tattle/route.js:3</sub>

_ Params: ?full=1    page everything instead of stopping at the watermark_

```js
//   ?days=45   overlap window for edited and late-arriving reviews
```
<sub>app/api/sync/tattle/route.js:12</sub>

_Params: ?full=1    page everything instead of stopping at the watermark ?days=45   overlap window for edited and late-arriving reviews_

```js
//   ?cer=1     also sync Tattle CER survey scores
```
<sub>app/api/sync/tattle/route.js:13</sub>

_?cer=1     also sync Tattle CER survey scores  Two things that look like bugs but are not:_

```js
//   - /online-reviews ignores every date param, so filtering is client side.
```
<sub>app/api/sync/tattle/route.js:16</sub>

_ Two things that look like bugs but are not: - /online-reviews ignores every date param, so filtering is client side._

```js
//   - review_date is the raw UTC date, never converted to store local time.
```
<sub>app/api/sync/tattle/route.js:17</sub>

_Two things that look like bugs but are not: - /online-reviews ignores every date param, so filtering is client side. - review_date is the raw UTC date, never converted to store local time._

```js
//     Yelp sends midnight timestamps, so converting pushes those reviews to
```
<sub>app/api/sync/tattle/route.js:18</sub>

```js
async function fetchPage(path, params) {
```
<sub>app/api/sync/tattle/route.js:32</sub>

```js
async function fetchLocations() {
```
<sub>app/api/sync/tattle/route.js:42</sub>

```js
function mapReview(r, storeIdByLocation) {
```
<sub>app/api/sync/tattle/route.js:52</sub>

```js
async function upsertChunked(table, rows, onConflict) {
```
<sub>app/api/sync/tattle/route.js:79</sub>

```js
async function syncReviews({ full, overlapDays, storeIdByLocation }) {
```
<sub>app/api/sync/tattle/route.js:90</sub>

```js
const json = await fetchPage('online-reviews', { page, pageSize: PAGE_SIZE });
```
<sub>app/api/sync/tattle/route.js:109</sub>

```js
async function syncCer({ storeIdByLocation, days }) {
```
<sub>app/api/sync/tattle/route.js:149</sub>

```js
b.sumAll += rating;
```
<sub>app/api/sync/tattle/route.js:175</sub>

```js
b.sumCafe += rating;
```
<sub>app/api/sync/tattle/route.js:178</sub>

```js
async function handler(request) {
```
<sub>app/api/sync/tattle/route.js:201</sub>

```js
export async function POST(request) {
```
<sub>app/api/sync/tattle/route.js:248</sub>

```js
export async function GET(request) {
```
<sub>app/api/sync/tattle/route.js:252</sub>

---

## `app/api/throughput/route.js`

```js
export async function GET(request) {
```
<sub>app/api/throughput/route.js:3</sub>

---

## `app/api/toast/cron-trigger/route.js`

```js
export async function GET(request) {
```
<sub>app/api/toast/cron-trigger/route.js:3</sub>

---

## `app/api/toast/cron/route.js`

```js
async function computeGrossSales(businessDate, restaurantGuid) {
```
<sub>app/api/toast/cron/route.js:5</sub>

```js
grossSales += (sel.preDiscountPrice || 0);
```
<sub>app/api/toast/cron/route.js:35</sub>

```js
return Math.round(grossSales * 100) / 100;
```
<sub>app/api/toast/cron/route.js:44</sub>

```js
async function syncOneStore(storeCode, restaurantGuid, businessDate, isoDate) {
```
<sub>app/api/toast/cron/route.js:47</sub>

```js
hours: (t.regularHours || 0) + (t.overtimeHours || 0),
```
<sub>app/api/toast/cron/route.js:61</sub>

```js
if (error) throw new Error("Labor store " + storeCode + ": " + error.message);
```
<sub>app/api/toast/cron/route.js:70</sub>

```js
if (salesErr) throw new Error("Sales store " + storeCode + ": " + salesErr.message);
```
<sub>app/api/toast/cron/route.js:80</sub>

```js
export async function GET(request) {
```
<sub>app/api/toast/cron/route.js:85</sub>

---

## `app/api/toast/jobs/route.js`

```js
export async function GET(request) {
```
<sub>app/api/toast/jobs/route.js:3</sub>

---

## `app/api/toast/labor-readable/route.js`

```js
export async function GET(request) {
```
<sub>app/api/toast/labor-readable/route.js:4</sub>

---

## `app/api/toast/labor/route.js`

```js
export async function GET(request) {
```
<sub>app/api/toast/labor/route.js:3</sub>

---

## `app/api/toast/orders-debug/route.js`

```js
export async function GET(request) {
```
<sub>app/api/toast/orders-debug/route.js:3</sub>

---

## `app/api/toast/sales-compare/route.js`

```js
async function computeGrossSales(businessDate, restaurantGuid) {
```
<sub>app/api/toast/sales-compare/route.js:5</sub>

```js
grossSales += (sel.preDiscountPrice || 0);
```
<sub>app/api/toast/sales-compare/route.js:36</sub>

```js
return Math.round(grossSales * 100) / 100;
```
<sub>app/api/toast/sales-compare/route.js:42</sub>

```js
export async function POST(request) {
```
<sub>app/api/toast/sales-compare/route.js:45</sub>

```js
hours: (t.regularHours || 0) + (t.overtimeHours || 0),
```
<sub>app/api/toast/sales-compare/route.js:73</sub>

```js
if (laborErr) throw new Error("Guardar labor fallo: " + laborErr.message);
```
<sub>app/api/toast/sales-compare/route.js:82</sub>

```js
if (salesErr) throw new Error("Guardar ventas fallo: " + salesErr.message);
```
<sub>app/api/toast/sales-compare/route.js:92</sub>

---

## `app/api/toast/sales-debug/route.js`

```js
export async function GET(request) {
```
<sub>app/api/toast/sales-debug/route.js:3</sub>

```js
grossSales += (sel.preDiscountPrice || sel.price || 0);
```
<sub>app/api/toast/sales-debug/route.js:60</sub>

```js
grossSales: Math.round(grossSales * 100) / 100,
```
<sub>app/api/toast/sales-debug/route.js:73</sub>

---

## `app/api/toast/sync-store/route.js`

```js
// Un ticket que tarda mas de esto no es lento, es que nunca se marco
```
<sub>app/api/toast/sync-store/route.js:7</sub>

_Toast manda fechas como 2026-08-19T13:45:12.000+0000. El offset sin dos puntos no es ISO 8601 estricto y Date.parse lo trata distinto segun el runtime. Lo normalizamos antes de parsear en vez de confiar en Node._

```js
function parseToastDate(value) {
```
<sub>app/api/toast/sync-store/route.js:19</sub>

_La hora se calcula en la zona horaria de LA TIENDA. Si se bucketeara en UTC o en Central fijo, la curva de Santa Monica saldria corrida 2 horas, y una curva corrida es peor que no tener curva porque el GM se la cree._

```js
function makeHourFormatter(timezone) {
```
<sub>app/api/toast/sync-store/route.js:29</sub>

```js
function localHour(formatter, date) {
```
<sub>app/api/toast/sync-store/route.js:37</sub>

_hora de reloj resulto, y la diferencia es el offset real de esa zona en ese instante. Nunca se le pide resolver las 2am de un cambio de horario (la unica hora ambigua o inexistente en un DST de EE.UU.): startOfLocalDay_

```js
// solo pide medianoche, que nunca cae en esa hora.
```
<sub>app/api/toast/sync-store/route.js:49</sub>

_ese instante. Nunca se le pide resolver las 2am de un cambio de horario (la unica hora ambigua o inexistente en un DST de EE.UU.): startOfLocalDay solo pide medianoche, que nunca cae en esa hora._

```js
function zonedWallClockToUtcMs(isoDate, timeStr, timeZone) {
```
<sub>app/api/toast/sync-store/route.js:50</sub>

```js
function addDaysIso(isoDate, n) {
```
<sub>app/api/toast/sync-store/route.js:72</sub>

_numerico de 4 digitos, no "Z". Mismo valor, mismo formato que Toast ya nos envia en sus propias fechas (ver parseToastDate arriba), para no introducir una forma que nunca se probo contra su API._

```js
function toToastUtcString(ms) {
```
<sub>app/api/toast/sync-store/route.js:82</sub>

```js
// Medianoche a medianoche en la zona real de la tienda, no un offset fijo.
```
<sub>app/api/toast/sync-store/route.js:86</sub>

_-0500 coincidia por casualidad con Texas/Tennessee en verano, pero es 2-3 horas incorrecto todo el año para California y Arizona, y se vuelve incorrecto para Texas/Tennessee tambien en cuanto termina el horario de_

```js
// verano. Un turno cerca de medianoche local ya se contaba bien en el
```
<sub>app/api/toast/sync-store/route.js:90</sub>

_reporte (report.js/throughput.js recalculan el dia real de cada turno con la zona de la tienda al leer), pero la ventana que se le pedia a Toast y la que decidia que fila borrar como huerfana no eran esa misma zona._

```js
function localDayWindowForToast(isoDate, timeZone) {
```
<sub>app/api/toast/sync-store/route.js:94</sub>

_Cuenta checks cerrados (no orders). Un check = una transaccion real de_

```js
// cliente pagando, misma unidad que usa gross sales. Si se contara por
```
<sub>app/api/toast/sync-store/route.js:104</sub>

_El desglose por hora sale del MISMO recorrido y con los MISMOS filtros de void/deleted/deferred. Si el hourly filtrara distinto que el daily, las dos vistas se pelearian y nadie sabria cual creer._

```js
async function computeSalesTransactionsAndHours(businessDate, restaurantGuid, token, timezone) {
```
<sub>app/api/toast/sync-store/route.js:110</sub>

```js
transactionCount += 1;
```
<sub>app/api/toast/sync-store/route.js:160</sub>

```js
checkSales += (sel.preDiscountPrice || 0);
```
<sub>app/api/toast/sync-store/route.js:168</sub>

```js
grossSales += checkSales;
```
<sub>app/api/toast/sync-store/route.js:171</sub>

```js
if (hour === null) unattributedSales += checkSales;
```
<sub>app/api/toast/sync-store/route.js:172</sub>

```js
else hourlySales[hour] += checkSales;
```
<sub>app/api/toast/sync-store/route.js:173</sub>

```js
grossSales: Math.round(grossSales * 100) / 100,
```
<sub>app/api/toast/sync-store/route.js:182</sub>

```js
unattributedSales: Math.round(unattributedSales * 100) / 100,
```
<sub>app/api/toast/sync-store/route.js:186</sub>

```js
function percentile(sorted, p) {
```
<sub>app/api/toast/sync-store/route.js:191</sub>

```js
function summarize(durationsMin, stuckCount) {
```
<sub>app/api/toast/sync-store/route.js:197</sub>

```js
medianMin: durationsMin.length ? Math.round(percentile(durationsMin, 0.5) * 10) / 10 : null,
```
<sub>app/api/toast/sync-store/route.js:201</sub>

```js
p90Min: durationsMin.length ? Math.round(percentile(durationsMin, 0.9) * 10) / 10 : null,
```
<sub>app/api/toast/sync-store/route.js:205</sub>

_Devuelve el agregado general Y el desglose por estacion de preparacion. El desglose es lo que le dice a un GM DONDE esta el cuello de botella, en vez de solo darle un numero general que no dice nada de por que._

```js
async function computeKitchenMetrics(businessDate, restaurantGuid, token) {
```
<sub>app/api/toast/sync-store/route.js:213</sub>

```js
export async function POST(request) {
```
<sub>app/api/toast/sync-store/route.js:266</sub>

_Marca de tiempo de ARRANQUE de esta corrida, capturada antes de tocar_

```js
// Toast o la base. Se usa mas abajo para decidir que filas de labor son
```
<sub>app/api/toast/sync-store/route.js:283</sub>

_decidir que filas borrar. Tienen que ser identicas, si no se borran turnos legitimos de la jornada anterior._

```js
// Medianoche a medianoche en la zona REAL de la tienda (ya resuelta
```
<sub>app/api/toast/sync-store/route.js:305</sub>

```js
hours: (t.regularHours || 0) + (t.overtimeHours || 0),
```
<sub>app/api/toast/sync-store/route.js:345</sub>

_El upsert va SIEMPRE primero. Con el orden anterior (leer huerfanos -> borrar -> upsert), un crash entre el borrado y el upsert dejaba a la_

```js
// tienda sin ninguna fila de labor para ese dia hasta el proximo sync -
```
<sub>app/api/toast/sync-store/route.js:352</sub>

```js
if (laborErr) throw new Error("Guardar labor fallo: " + laborErr.message);
```
<sub>app/api/toast/sync-store/route.js:371</sub>

```js
if (salesErr) throw new Error("Guardar ventas fallo: " + salesErr.message);
```
<sub>app/api/toast/sync-store/route.js:390</sub>

_Transacciones van en su PROPIA tabla y en su propio try/catch, igual_

```js
// que cocina. Si daily_transactions no existe todavia o falla la escritura,
```
<sub>app/api/toast/sync-store/route.js:393</sub>

_Transacciones van en su PROPIA tabla y en su propio try/catch, igual que cocina. Si daily_transactions no existe todavia o falla la escritura,_

```js
// esto NO tumba labor ni ventas. El reporte diario (Dia/WTD/PTD) no
```
<sub>app/api/toast/sync-store/route.js:394</sub>

```js
// labor y ventas ya son un hecho consumado si llegamos aca: cualquier
```
<sub>app/api/toast/sync-store/route.js:489</sub>

_labor y ventas ya son un hecho consumado si llegamos aca: cualquier falla en esas dos tira un throw mas arriba y corta todo antes de_

```js
// escribir nada (comportamiento sin cambios). transactions y hourly
```
<sub>app/api/toast/sync-store/route.js:491</sub>

_SI pueden fallar sin cortar el resto (kitchen tambien, y ademas puede saltarse a proposito) - pero un ok:true con esos dos en error dejaba creer que la corrida cubrio esta tienda cuando en realidad faltan_

```js
// datos que el TPLH y la curva por hora si necesitan. La verificacion de
```
<sub>app/api/toast/sync-store/route.js:495</sub>

---

## `app/api/toast/sync/route.js`

```js
function calcHours(inTs, outTs) {
```
<sub>app/api/toast/sync/route.js:4</sub>

```js
function mapEntry(entry, storeGuid) {
```
<sub>app/api/toast/sync/route.js:10</sub>

```js
export async function POST(request) {
```
<sub>app/api/toast/sync/route.js:25</sub>

---

## `app/api/toast/token/route.js`

```js
export async function GET() {
```
<sub>app/api/toast/token/route.js:3</sub>

```js
preview: token.slice(0, 12) + "...",
```
<sub>app/api/toast/token/route.js:9</sub>

---

## `app/api/whoami/route.js`

```js
export async function GET() {
```
<sub>app/api/whoami/route.js:10</sub>

---

## `components/CupMedal.js`

_Lengthwise sheen: dark edge, light core, dark edge. Reads as struck metal._

```js
function Sheen({ id, f, shift = 38 }) {
```
<sub>components/CupMedal.js:50</sub>

```js
{/* engraved rank */}
```
<sub>components/CupMedal.js:112</sub>

---

## `components/Dashboard.js`

```js
// The top review line, read from scale.js rather than typed. It used to appear
```
<sub>components/Dashboard.js:43</sub>

_A number that means something, and a place to go do something about it._

```js
function Tile({ label, value, unit, note, tone, onClick, to }) {
```
<sub>components/Dashboard.js:49</sub>

```js
function SectionHead({ title, sub, action, onAction }) {
```
<sub>components/Dashboard.js:66</sub>

```js
function pickScope(key) {
```
<sub>components/Dashboard.js:96</sub>

```js
// ---- labor ----
```
<sub>components/Dashboard.js:137</sub>

```js
curH += r.wtd.hours;
```
<sub>components/Dashboard.js:140</sub>

```js
curS += r.wtd.sales;
```
<sub>components/Dashboard.js:141</sub>

```js
const blended = curH > 0 ? Math.round(curS / curH) : null;
```
<sub>components/Dashboard.js:143</sub>

```js
// The delta is computed only across stores that reported both weeks, and on
```
<sub>components/Dashboard.js:145</sub>

_The delta is computed only across stores that reported both weeks, and on_

```js
// summed hours and sales rather than an average of ratios.
```
<sub>components/Dashboard.js:146</sub>

```js
cH += t.curHours;
```
<sub>components/Dashboard.js:150</sub>

```js
cS += t.curSales;
```
<sub>components/Dashboard.js:151</sub>

```js
pH += t.priHours;
```
<sub>components/Dashboard.js:152</sub>

```js
pS += t.priSales;
```
<sub>components/Dashboard.js:153</sub>

```js
const delta = cH > 0 && pH > 0 ? Math.round(cS / cH - pS / pH) : null;
```
<sub>components/Dashboard.js:155</sub>

```js
const atTarget = rows.filter((r) => r.wtd.hours > 0 && r.wtd.splh >= r.day.target).length;
```
<sub>components/Dashboard.js:157</sub>

```js
// ---- reviews ----
```
<sub>components/Dashboard.js:159</sub>

```js
(r) => r.reviews?.period?.rating != null && r.reviews.period.count >= MIN_REVIEWS_TO_RANK
```
<sub>components/Dashboard.js:162</sub>

```js
.sort((a, b) => a.reviews.period.rating - b.reviews.period.rating)
```
<sub>components/Dashboard.js:166</sub>

_Same scale logic as the Leaderboard goal bar, so 100% lines up in both_

```js
// places: the higher of 120 or the top performer's own efficiency, rounded
```
<sub>components/Dashboard.js:173</sub>

_Two short columns read faster than one list of diverging bars: a glance at "who's up" and "who's down" without decoding bar length against a shared axis._

```js
const movers = [...comps].sort((a, b) => b.delta - a.delta);
```
<sub>components/Dashboard.js:183</sub>

```js
const down = movers.filter((t) => t.delta < 0).slice(-5).reverse();
```
<sub>components/Dashboard.js:185</sub>

```js
{blended === null ? "--" : "$" + blended}
```
<sub>components/Dashboard.js:230</sub>

```js
{delta >= 0 ? "+" : ""}
```
<sub>components/Dashboard.js:239</sub>

```js
tone={rows.length && atTarget / rows.length >= 0.6 ? "pos" : "neg"}
```
<sub>components/Dashboard.js:270</sub>

```js
label={"Guest rating · period " + data.period}
```
<sub>components/Dashboard.js:275</sub>

```js
value={chain.rating === null ? "--" : chain.rating.toFixed(2)}
```
<sub>components/Dashboard.js:276</sub>

```js
: chain.rating >= TOP_LINE
```
<sub>components/Dashboard.js:285</sub>

```js
: chain.rating >= REVIEW_TIERS.baseOnly
```
<sub>components/Dashboard.js:287</sub>

```js
note={money(curS) + " in sales behind them"}
```
<sub>components/Dashboard.js:297</sub>

```js
{Math.round(s.eff)}% eff
```
<sub>components/Dashboard.js:323</sub>

```js
{s.rev.rating.toFixed(2)}
```
<sub>components/Dashboard.js:328</sub>

```js
{rev.rating.toFixed(2)}
```
<sub>components/Dashboard.js:480</sub>

```js
{rev.rating >= REVIEW_TIERS.baseOnly
```
<sub>components/Dashboard.js:482</sub>

```js
? `+${(TOP_LINE - rev.rating).toFixed(2)} to ${TOP_LINE.toFixed(2)}`
```
<sub>components/Dashboard.js:483</sub>

```js
: `under ${REVIEW_TIERS.baseOnly.toFixed(2)}`}
```
<sub>components/Dashboard.js:484</sub>

---

## `components/DriveThru.js`

_Single view, progressive disclosure. Click a store card to switch stores, click a day in the calendar to drill into that day's hour-by-hour. Once a day is selected, the headline banner, eyebrow, and the four KPI cards all_

```js
// scope to that single day instead of the window average.
```
<sub>components/DriveThru.js:7</sub>

_scope to that single day instead of the window average.  Every color and every threshold on this screen comes from lib/scale.js and_

```js
// metric_targets. There is no goal number and no hex in this file, which is
```
<sub>components/DriveThru.js:10</sub>

_ Every color and every threshold on this screen comes from lib/scale.js and metric_targets. There is no goal number and no hex in this file, which is_

```js
// why moving the target from 45s to 1:45 was a database update rather than a
```
<sub>components/DriveThru.js:11</sub>

```js
function shortDate(iso) {
```
<sub>components/DriveThru.js:33</sub>

_"FRI, AUG 14" — used by the eyebrow once a single day is selected._

```js
function dayLabel(iso) {
```
<sub>components/DriveThru.js:39</sub>

```js
function mondayOf(iso) {
```
<sub>components/DriveThru.js:46</sub>

```js
function dowIndex(iso) {
```
<sub>components/DriveThru.js:54</sub>

```js
function onKeyDown(e) {
```
<sub>components/DriveThru.js:98</sub>

```js
function toggleDay(date) {
```
<sub>components/DriveThru.js:130</sub>

```js
hourAgg[h].sum += (r.avg_window_time || 0) * (r.car_count || 0);
```
<sub>components/DriveThru.js:153</sub>

```js
hourAgg[h].menu += (r.avg_menu_time || 0) * (r.car_count || 0);
```
<sub>components/DriveThru.js:154</sub>

```js
hourAgg[h].queue += (r.avg_cars_in_queue || 0) * (r.car_count || 0);
```
<sub>components/DriveThru.js:155</sub>

```js
avg: hourAgg[h].n ? hourAgg[h].sum / hourAgg[h].n : null,
```
<sub>components/DriveThru.js:163</sub>

_A single hour can't be "toughest" or "best" against nothing, so a day_

```js
// with fewer than two distinct hours of data gets a placeholder instead of
```
<sub>components/DriveThru.js:173</sub>

```js
// Distribution. The band ships with each row, so this component never has to
```
<sub>components/DriveThru.js:177</sub>

_Distribution. The band ships with each row, so this component never has to_

```js
// work out which buckets count as at-target. This stays a whole-window,
```
<sub>components/DriveThru.js:178</sub>

_Distribution. The band ships with each row, so this component never has to work out which buckets count as at-target. This stays a whole-window, whole-store figure: there is no per-day distribution endpoint, and the_

```js
// day view below uses the daily row's own pct_green instead.
```
<sub>components/DriveThru.js:180</sub>

```js
const distTotal = distRows.reduce((a, r) => a + r.car_count, 0);
```
<sub>components/DriveThru.js:184</sub>

```js
const pctAtGoal = distTotal ? (100 * atGoalCars) / distTotal : 0;
```
<sub>components/DriveThru.js:188</sub>

```js
const gap = activeAvgWindowTime != null ? activeAvgWindowTime - cfg.target : null;
```
<sub>components/DriveThru.js:214</sub>

```js
)} target, with ${activeAtGoalPct.toFixed(0)}% of cars making it.`
```
<sub>components/DriveThru.js:221</sub>

```js
)} over the ${fmtSeconds(cfg.target)} target, with ${activeAtGoalPct.toFixed(
```
<sub>components/DriveThru.js:224</sub>

```js
{activeAtGoalPct.toFixed(0)}%
```
<sub>components/DriveThru.js:384</sub>

```js
{(100 - activeAtGoalPct).toFixed(0)}% still over {fmtSeconds(cfg.target)}
```
<sub>components/DriveThru.js:387</sub>

```js
median <strong>{fmtSeconds(selectedDayRow.median_window_time)}</strong>
```
<sub>components/DriveThru.js:608</sub>

```js
{/* Distribution. Buckets and their bands are computed in the view, so the
```
<sub>components/DriveThru.js:653</sub>

```js
const p = distTotal ? (100 * r.car_count) / distTotal : 0;
```
<sub>components/DriveThru.js:670</sub>

---

## `components/EfficiencyQuadrant.js`

```js
function efficiency(row) {
```
<sub>components/EfficiencyQuadrant.js:6</sub>

```js
return ((h + row.wtd.overUnder) / h) * 100;
```
<sub>components/EfficiencyQuadrant.js:9</sub>

```js
function quadrantOf(eff, ticketMin, effMid, ticketMid) {
```
<sub>components/EfficiencyQuadrant.js:12</sub>

```js
if (eff >= effMid && ticketMin >= ticketMid) return "cutting";
```
<sub>components/EfficiencyQuadrant.js:13</sub>

```js
if (eff >= effMid && ticketMin < ticketMid) return "model";
```
<sub>components/EfficiencyQuadrant.js:14</sub>

```js
function CustomTip({ active, payload }) {
```
<sub>components/EfficiencyQuadrant.js:26</sub>

---

## `components/EmailPreview.js`

```js
async function copyHtml() {
```
<sub>components/EmailPreview.js:26</sub>

---

## `components/Forecast.js`

```js
function mondayOf(iso) {
```
<sub>components/Forecast.js:5</sub>

```js
function shiftWeek(iso, n) {
```
<sub>components/Forecast.js:12</sub>

```js
function prettyDate(iso) {
```
<sub>components/Forecast.js:18</sub>

_Cortes redondos para el eje. Se apunta a 5 divisiones: menos deja el eje pobre, mas lo satura. El tope se estira al siguiente corte para que la barra mas alta no toque el techo._

```js
function niceStep(rough) {
```
<sub>components/Forecast.js:41</sub>

```js
function buildAxis(max) {
```
<sub>components/Forecast.js:53</sub>

```js
function money(v) {
```
<sub>components/Forecast.js:62</sub>

```js
function HourlyCurve({ day, maxStaffCapacity, openFrom, openTo }) {
```
<sub>components/Forecast.js:66</sub>

```js
const hours = day.hours.filter((h) => h.hour >= openFrom && h.hour <= openTo);
```
<sub>components/Forecast.js:78</sub>

```js
const gridMin = GUTTER + hours.length * COL_MIN;
```
<sub>components/Forecast.js:80</sub>

_Las barras miden VENTA, no personal. El personal viene de dividir la_

```js
// venta entre el target y redondear a entero, asi que si las barras se
```
<sub>components/Forecast.js:83</sub>

_Las barras miden VENTA, no personal. El personal viene de dividir la venta entre el target y redondear a entero, asi que si las barras se escalaran con el, las alturas no cuadrarian contra un eje en dolares._

```js
const maxSales = hours.reduce((a, h) => Math.max(a, h.expectedSales), 0);
```
<sub>components/Forecast.js:85</sub>

```js
? Math.max(3, (h.expectedSales / axis.top) * CHART_H)
```
<sub>components/Forecast.js:160</sub>

```js
aria-label={`${h.label}, ${h.staff} people, ${money(h.expectedSales)} expected`}
```
<sub>components/Forecast.js:167</sub>

```js
function loadData() {
```
<sub>components/Forecast.js:267</sub>

```js
return fetch(`/api/forecast?store=${storeCode}&weekStart=${weekStart}`)
```
<sub>components/Forecast.js:272</sub>

_Se pide una sola vez, al abrir el primer dia. Quien nunca expande una fila no paga la consulta._

```js
function toggleDay(date) {
```
<sub>components/Forecast.js:302</sub>

```js
fetch(`/api/forecast/hourly?store=${storeCode}&weekStart=${weekStart}`)
```
<sub>components/Forecast.js:309</sub>

```js
function setDay(date, value) {
```
<sub>components/Forecast.js:319</sub>

```js
function useAllowed() {
```
<sub>components/Forecast.js:324</sub>

```js
async function savePlan() {
```
<sub>components/Forecast.js:334</sub>

```js
const res = await fetch("/api/forecast", {
```
<sub>components/Forecast.js:344</sub>

```js
const r = await fetch(`/api/forecast?store=${storeCode}&weekStart=${weekStart}`);
```
<sub>components/Forecast.js:352</sub>

_Borra un dia guardado. Distinto a dejar el campo vacio, que solo lo ignora al guardar sin quitar lo que ya estaba en la base._

```js
async function deleteDay(date) {
```
<sub>components/Forecast.js:363</sub>

```js
const res = await fetch(`/api/forecast?store=${storeCode}&date=${date}`, { method: "DELETE" });
```
<sub>components/Forecast.js:366</sub>

```js
async function clearWeek() {
```
<sub>components/Forecast.js:378</sub>

```js
const res = await fetch(`/api/forecast?store=${storeCode}&weekStart=${weekStart}`, { method: "DELETE" });
```
<sub>components/Forecast.js:381</sub>

```js
// Totales en vivo mientras escriben, sin esperar a guardar
```
<sub>components/Forecast.js:390</sub>

```js
const liveVar = Math.round(livePlanned - t.allowedHours);
```
<sub>components/Forecast.js:431</sub>

```js
${t.expectedSales.toLocaleString("en-US")}
```
<sub>components/Forecast.js:460</sub>

```js
? (liveVar > 0 ? `${liveVar} hours over` : liveVar < 0 ? `${Math.abs(liveVar)} hours under` : "right on budget")
```
<sub>components/Forecast.js:476</sub>

```js
const diff = val !== "" && isFinite(n) ? Math.round(n - d.allowedHours) : null;
```
<sub>components/Forecast.js:532</sub>

```js
${d.expectedSales.toLocaleString("en-US")}
```
<sub>components/Forecast.js:567</sub>

```js
${d.minSales.toLocaleString("en-US")} – ${d.maxSales.toLocaleString("en-US")} · target ${d.target}
```
<sub>components/Forecast.js:570</sub>

---

## `components/Icon.js`

```js
rank: "M3 20h18|M6 20v-5|M12 20V5|M18 20v-8",
```
<sub>components/Icon.js:8</sub>

---

## `components/KitchenTrend.js`

```js
// Escala inversa a la de SPLH: aqui MENOS tiempo es mejor.
```
<sub>components/KitchenTrend.js:7</sub>

_Escala inversa a la de SPLH: aqui MENOS tiempo es mejor._

```js
function cellStyle(min) {
```
<sub>components/KitchenTrend.js:8</sub>

```js
function barColor(min) {
```
<sub>components/KitchenTrend.js:18</sub>

```js
function prettyFull(iso) {
```
<sub>components/KitchenTrend.js:25</sub>

```js
function DayPanel({ storeCode, date, onClose }) {
```
<sub>components/KitchenTrend.js:31</sub>

```js
const maxMedian = stations.reduce((m, s) => Math.max(m, s.median_minutes || 0), 0) || 1;
```
<sub>components/KitchenTrend.js:71</sub>

```js
? Math.round((slowest.median_minutes / fastest.median_minutes) * 10) / 10
```
<sub>components/KitchenTrend.js:75</sub>

```js
{s.median_minutes !== null ? s.median_minutes.toFixed(1) : "—"}
```
<sub>components/KitchenTrend.js:131</sub>

```js
{data.overall.medianMin !== null ? data.overall.medianMin.toFixed(1) : "—"}
```
<sub>components/KitchenTrend.js:205</sub>

```js
{w.totals.medianMin !== null ? w.totals.medianMin.toFixed(1) + "m" : "—"}
```
<sub>components/KitchenTrend.js:244</sub>

```js
{d.medianMin.toFixed(1)}
```
<sub>components/KitchenTrend.js:270</sub>

```js
{b.hasData ? b.medianMin.toFixed(1) : "–"}
```
<sub>components/KitchenTrend.js:290</sub>

---

## `components/Leaderboard.js`

```js
blurb: `Google and Yelp average, ${RATING_TARGET.toFixed(2)} is the line`,
```
<sub>components/Leaderboard.js:57</sub>

```js
// Period first. It is the wider window and the only one with enough reviews
```
<sub>components/Leaderboard.js:72</sub>

_Progress toward the store's own line, with that line marked on the track._

```js
function GoalBar({ value, target, scale }) {
```
<sub>components/Leaderboard.js:82</sub>

```js
const mark = Math.min(100, (target / scale) * 100);
```
<sub>components/Leaderboard.js:85</sub>

_Star average colored by the review cutoffs. The thresholds live in lib/scale.js, never here. /_

```js
function Rating({ rev, showCount = true }) {
```
<sub>components/Leaderboard.js:101</sub>

```js
const d = (board.rankBy(b) ?? -Infinity) - (board.rankBy(a) ?? -Infinity);
```
<sub>components/Leaderboard.js:155</sub>

```js
ordered.forEach((s, i) => (rank[s.code] = i + 1));
```
<sub>components/Leaderboard.js:161</sub>

_Header numbers. Both are counts of stores clearing their own line, which_

```js
// is what a GM is graded on. They are deliberately not blended.
```
<sub>components/Leaderboard.js:168</sub>

_Header numbers. Both are counts of stores clearing their own line, which is what a GM is graded on. They are deliberately not blended._

```js
const atSplh = rows.filter((s) => s.eff >= 100).length;
```
<sub>components/Leaderboard.js:169</sub>

```js
const atRating = rated.filter((s) => s.rev.rating >= RATING_TARGET).length;
```
<sub>components/Leaderboard.js:171</sub>

_Returns text and color rather than an element, because the podium and the list wrap it in different classes and a nested span would inherit both. /_

```js
function boardValue(store) {
```
<sub>components/Leaderboard.js:186</sub>

_The slot to the left of the big number. Held open on every board even when it is empty, so the row grid does not reflow when you switch tabs. /_

```js
function BoardAside({ store }) {
```
<sub>components/Leaderboard.js:202</sub>

```js
if (boardKey === "score") return <Rating rev={store.rev} />;
```
<sub>components/Leaderboard.js:203</sub>

```js
function metaOf(store) {
```
<sub>components/Leaderboard.js:214</sub>

```js
return `${Math.round(store.eff)}% eff · $${store.splh} SPLH`;
```
<sub>components/Leaderboard.js:217</sub>

```js
{RATING_TARGET.toFixed(2)} or better, {windowLabel}
```
<sub>components/Leaderboard.js:246</sub>

```js
{medRating === null ? "--" : medRating.toFixed(2)}
```
<sub>components/Leaderboard.js:255</sub>

```js
{detail.rev?.rating != null ? detail.rev.rating.toFixed(2) : "--"}
```
<sub>components/Leaderboard.js:366</sub>

```js
{detail.eff >= 100
```
<sub>components/Leaderboard.js:375</sub>

```js
? `Beating the labor target by ${Math.round(detail.eff - 100)} points. `
```
<sub>components/Leaderboard.js:376</sub>

```js
: `Short of the labor target by ${Math.round(100 - detail.eff)} points. `}
```
<sub>components/Leaderboard.js:377</sub>

```js
: detail.rev.rating >= RATING_TARGET
```
<sub>components/Leaderboard.js:382</sub>

```js
? `Guests are rating ${detail.rev.rating.toFixed(2)} across ${detail.rev.count} reviews, at or above the ${RATING_TARGET.toFixed(2)} line.`
```
<sub>components/Leaderboard.js:383</sub>

```js
: `Guests are rating ${detail.rev.rating.toFixed(2)} across ${detail.rev.count} reviews, ${(RATING_TARGET - detail.rev.rating).toFixed(2)} under the ${RATING_TARGET.toFixed(2)} line.`}
```
<sub>components/Leaderboard.js:384</sub>

```js
? `The line on each bar is ${RATING_TARGET.toFixed(2)}. Click a row for the detail.`
```
<sub>components/Leaderboard.js:397</sub>

---

## `components/ServiceBoard.js`

```js
function bandOf(store, med, th) {
```
<sub>components/ServiceBoard.js:23</sub>

```js
const r = store.medianMin / med;
```
<sub>components/ServiceBoard.js:24</sub>

_Business rule kept from the API: nothing under the noticeable floor is ever called slow, however bad the ratio looks._

```js
if (store.medianMin >= th.noticeableFloorMin) {
```
<sub>components/ServiceBoard.js:27</sub>

```js
function confidenceOf(store, th) {
```
<sub>components/ServiceBoard.js:34</sub>

```js
function Section({ title, count, children, note }) {
```
<sub>components/ServiceBoard.js:44</sub>

```js
function ReasonList({ stores }) {
```
<sub>components/ServiceBoard.js:67</sub>

```js
.filter((s) => s.medianMin >= PLAUSIBLE_MIN)
```
<sub>components/ServiceBoard.js:139</sub>

```js
.sort((a, b) => a.medianMin - b.medianMin);
```
<sub>components/ServiceBoard.js:140</sub>

```js
data-label={`median ${dec(med, 1)}`}
```
<sub>components/ServiceBoard.js:236</sub>

```js
left: railPos(s.medianMin, lo, hi) + "%",
```
<sub>components/ServiceBoard.js:244</sub>

```js
const ratio = s.medianMin / med;
```
<sub>components/ServiceBoard.js:303</sub>

```js
.sort((a, b) => a.medianMin - b.medianMin)
```
<sub>components/ServiceBoard.js:364</sub>

---

## `components/ShiftLogo.js`

```js
// Fillet, modelled on the Janus bust: a straight braided band across both
```
<sub>components/ShiftLogo.js:35</sub>

_in the opposite value of the half it crosses, light across the dark face and dark across the light one, so it reads as one piece worn by both heads and never needs an outline. The notches are painted in the face color underneath,_

```js
// which is what gives the band its braided texture for free.
```
<sub>components/ShiftLogo.js:40</sub>

```js
function notchRects(x0, x1, color) {
```
<sub>components/ShiftLogo.js:46</sub>

```js
y={BAND_Y - 0.2}
```
<sub>components/ShiftLogo.js:52</sub>

```js
function Crown({ on, left = FACE_R, right = FACE_L, faceLeft = FACE_L, faceRight = FACE_R }) {
```
<sub>components/ShiftLogo.js:63</sub>

```js
{notchRects(32.3, 64 - BAND_X, faceRight)}
```
<sub>components/ShiftLogo.js:69</sub>

```js
function Faces({ left = FACE_L, right = FACE_R, eye = EYE, showEyes = true }) {
```
<sub>components/ShiftLogo.js:78</sub>

---

## `components/StoreTrend.js`

```js
// TICKET TIMES DISABLED: Toast Expo data is not reliable yet. Restore this import
```
<sub>components/StoreTrend.js:6</sub>

_TICKET TIMES DISABLED: Toast Expo data is not reliable yet. Restore this import_

```js
// along with the three blocks marked TICKET TIMES DISABLED below.
```
<sub>components/StoreTrend.js:7</sub>

```js
function cellStyle(ratio) {
```
<sub>components/StoreTrend.js:12</sub>

```js
function prettyFull(iso) {
```
<sub>components/StoreTrend.js:22</sub>

```js
function ChartTip({ active, payload }) {
```
<sub>components/StoreTrend.js:28</sub>

```js
function DayPanel({ day, onClose }) {
```
<sub>components/StoreTrend.js:40</sub>

```js
{Math.abs(day.overUnder)}
```
<sub>components/StoreTrend.js:71</sub>

```js
: `Sales of $${Math.round(day.sales).toLocaleString("en-US")} at a $${day.target} target supported about ${Math.round(day.sales / day.target)} hours, and you used ${day.hours}. Good day.`}
```
<sub>components/StoreTrend.js:78</sub>

```js
{/* TICKET TIMES DISABLED: was (tab === "history" || tab === "kitchen") */}
```
<sub>components/StoreTrend.js:140</sub>

```js
{/* TICKET TIMES DISABLED: restore this button to bring the sub tab back.
```
<sub>components/StoreTrend.js:172</sub>

```js
{/* TICKET TIMES DISABLED: restore this block along with the button above.
```
<sub>components/StoreTrend.js:213</sub>

_The three summary tiles that used to sit here have been removed. A manager opening this screen wants to know which days went wrong, and the grid below_

```js
// answers that directly. Restating the same four week totals in a different
```
<sub>components/StoreTrend.js:234</sub>

```js
{w.totals.splh > 0 ? "$" + w.totals.splh : "—"}
```
<sub>components/StoreTrend.js:285</sub>

```js
{b.hasData ? "$" + b.splh : "–"}
```
<sub>components/StoreTrend.js:331</sub>

```js
{w.totals.splh > 0 ? "$" + w.totals.splh : "—"}
```
<sub>components/StoreTrend.js:390</sub>

```js
{Math.round(w.totals.hours).toLocaleString("en-US")} hrs &middot; ${Math.round(w.totals.sales).toLocaleString("en-US")}
```
<sub>components/StoreTrend.js:396</sub>

```js
? Math.abs(w.totals.overUnder) + " hrs over budget"
```
<sub>components/StoreTrend.js:399</sub>

```js
: w.totals.overUnder + " hrs under budget"}
```
<sub>components/StoreTrend.js:400</sub>

---

## `components/TPLH.js`

```js
label: "Volume + ticket",
```
<sub>components/TPLH.js:31</sub>

```js
label: "Ticket-led",
```
<sub>components/TPLH.js:41</sub>

```js
function mixOf(tplh, avgTicket, cTplh, cTicket) {
```
<sub>components/TPLH.js:53</sub>

```js
const t = tplh / cTplh;
```
<sub>components/TPLH.js:55</sub>

```js
const k = avgTicket / cTicket;
```
<sub>components/TPLH.js:56</sub>

```js
function Delta({ value }) {
```
<sub>components/TPLH.js:68</sub>

```js
const totalTxn = rows.reduce((a, r) => a + (r.transactions || 0), 0);
```
<sub>components/TPLH.js:157</sub>

```js
const totalSales = rows.reduce((a, r) => a + (r.sales || 0), 0);
```
<sub>components/TPLH.js:158</sub>

```js
const companyTicket = totalTxn > 0 ? totalSales / totalTxn : null;
```
<sub>components/TPLH.js:159</sub>

```js
const ranked = [...reporting].sort((a, b) => b.tplh - a.tplh);
```
<sub>components/TPLH.js:172</sub>

```js
ranked.forEach((r, i) => (rank[r.code] = i + 1));
```
<sub>components/TPLH.js:174</sub>

```js
data-label={`median ${dec(med)}`}
```
<sub>components/TPLH.js:272</sub>

```js
left: railPos(r.tplh, lo, hi) + "%",
```
<sub>components/TPLH.js:282</sub>

```js
// Light bands would vanish against the rail without this.
```
<sub>components/TPLH.js:284</sub>

```js
const wow = last && s.tplh ? ((s.tplh - last) / last) * 100 : null;
```
<sub>components/TPLH.js:385</sub>

```js
{s.avgTicket === null ? "—" : "$" + dec(s.avgTicket)}
```
<sub>components/TPLH.js:436</sub>

```js
const wow = last && s.tplh ? ((s.tplh - last) / last) * 100 : null;
```
<sub>components/TPLH.js:465</sub>

```js
{s.avgTicket === null ? "—" : "$" + dec(s.avgTicket)}
```
<sub>components/TPLH.js:509</sub>

```js
{s.splh === null ? "—" : "$" + dec(s.splh, 0)}
```
<sub>components/TPLH.js:533</sub>

---

## `components/Targets.js`

```js
async function save(st) {
```
<sub>components/Targets.js:41</sub>

---

## `components/WeekOverWeekChart.js`

```js
function CustomTooltip({ active, payload }) {
```
<sub>components/WeekOverWeekChart.js:5</sub>

```js
const up = d.delta >= 0;
```
<sub>components/WeekOverWeekChart.js:8</sub>

```js
{up ? "▲" : "▼"} {Math.abs(d.delta)} vs last week
```
<sub>components/WeekOverWeekChart.js:25</sub>

```js
function DeltaLabel(props) {
```
<sub>components/WeekOverWeekChart.js:31</sub>

```js
.sort((a, b) => b.delta - a.delta);
```
<sub>components/WeekOverWeekChart.js:54</sub>

---

## `components/WeekView.js`

```js
gap: (s) => s.day.splh - s.day.target,
```
<sub>components/WeekView.js:19</sub>

```js
wtdGap: (s) => s.wtd.splh - s.day.target,
```
<sub>components/WeekView.js:24</sub>

_approach that is never ambiguous. /_

```js
function Th({ label, sortKey, sort, onSort, className = "" }) {
```
<sub>components/WeekView.js:55</sub>

```js
function Flag({ flags }) {
```
<sub>components/WeekView.js:72</sub>

_Background for a rating cell. Thin samples get no color at all, so nobody reads a red cell built on two reviews as a real result. /_

```js
function ratingCellStyle(rev) {
```
<sub>components/WeekView.js:85</sub>

```js
function onSort(key) {
```
<sub>components/WeekView.js:95</sub>

```js
(a, r) => ({ hours: a.hours + r.day.hours, sales: a.sales + r.day.sales }),
```
<sub>components/WeekView.js:127</sub>

```js
const totalSplh = totals.hours > 0 ? Math.round(totals.sales / totals.hours) : 0;
```
<sub>components/WeekView.js:130</sub>

```js
// Chain rating for the period, weighted by review count so a store with
```
<sub>components/WeekView.js:132</sub>

_Chain rating for the period, weighted by review count so a store with_

```js
// four reviews does not swing the company number. Company-wide by design,
```
<sub>components/WeekView.js:133</sub>

```js
background: "var(--band-green)",
```
<sub>components/WeekView.js:167</sub>

```js
Labor Dashboard - {report.dayName}, {report.date}
```
<sub>components/WeekView.js:226</sub>

```js
P{report.period} {chainRating.toFixed(2)} ★
```
<sub>components/WeekView.js:238</sub>

```js
label={<>WTD (Over)/<br />Under</>}
```
<sub>components/WeekView.js:291</sub>

```js
{rev.rating.toFixed(2)}
```
<sub>components/WeekView.js:385</sub>

```js
Guest Reviews - Period {report.period}
```
<sub>components/WeekView.js:498</sub>

---

## `lib/aggregate.js`

```js
// Agregacion WTD y PTD — portado de la logica en genEmailHTML / renderWeek de SHIFT.
```
<sub>lib/aggregate.js:1</sub>

```js
// ── WTD: suma horas/ventas/training de todos los dias cargados de la semana ──
```
<sub>lib/aggregate.js:5</sub>

_── WTD: suma horas/ventas/training de todos los dias cargados de la semana ──_

```js
// dailyData: { [dayName]: { [code]: { hours, sales, trainee, trainer } } }
```
<sub>lib/aggregate.js:6</sub>

_── WTD: suma horas/ventas/training de todos los dias cargados de la semana ── dailyData: { [dayName]: { [code]: { hours, sales, trainee, trainer } } } loadedDays: array de nombres de dia ya cargados, ej ["Monday","Tuesday",...]_

```js
export function aggregateWTD(dailyData, loadedDays, store) {
```
<sub>lib/aggregate.js:8</sub>

```js
wHours += d.hours || 0;
```
<sub>lib/aggregate.js:13</sub>

```js
wSales += d.sales || 0;
```
<sub>lib/aggregate.js:14</sub>

```js
wOverUnder += (d.sales / t) - d.hours;
```
<sub>lib/aggregate.js:16</sub>

```js
wTrainee += (d.trainee || 0);
```
<sub>lib/aggregate.js:18</sub>

```js
wTrainer += (d.trainer || 0);
```
<sub>lib/aggregate.js:19</sub>

```js
const wSPLH = wHours > 0 ? wSales / wHours : 0;
```
<sub>lib/aggregate.js:21</sub>

```js
const target = loadedDays.length ? getTarget(store, loadedDays[loadedDays.length - 1]) : 0;
```
<sub>lib/aggregate.js:22</sub>

```js
ok: wSPLH >= target,
```
<sub>lib/aggregate.js:28</sub>

```js
// ── PTD: totales de periodo a la fecha (viene ya agregado de la tabla, o se suma dia a dia) ──
```
<sub>lib/aggregate.js:35</sub>

_── PTD: totales de periodo a la fecha (viene ya agregado de la tabla, o se suma dia a dia) ──_

```js
// ptdRaw: { hours, sales } acumulado del periodo fiscal actual para esa tienda
```
<sub>lib/aggregate.js:36</sub>

_── PTD: totales de periodo a la fecha (viene ya agregado de la tabla, o se suma dia a dia) ── ptdRaw: { hours, sales } acumulado del periodo fiscal actual para esa tienda_

```js
export function ptdMetrics(ptdRaw, store) {
```
<sub>lib/aggregate.js:37</sub>

```js
if (!ptdRaw || (ptdRaw.hours <= 0 && ptdRaw.sales <= 0)) {
```
<sub>lib/aggregate.js:38</sub>

```js
const splh = ptdRaw.hours > 0 ? ptdRaw.sales / ptdRaw.hours : 0;
```
<sub>lib/aggregate.js:42</sub>

```js
return { hours: ptdRaw.hours, sales: ptdRaw.sales, splh, ok: splh >= target, empty: false };
```
<sub>lib/aggregate.js:43</sub>

---

## `lib/auth.js`

_El middleware ya verifico la firma y escribio estas cabeceras, y borro cualquiera que viniera del cliente antes de escribirlas. /_

```js
export function sessionFrom(request) {
```
<sub>lib/auth.js:18</sub>

```js
export function requireRole(request, roles) {
```
<sub>lib/auth.js:33</sub>

---

## `lib/cache.js`

```js
export function memo(key, ttlMs, fn) {
```
<sub>lib/cache.js:17</sub>

```js
export function invalidate(prefix) {
```
<sub>lib/cache.js:38</sub>

---

## `lib/calc.js`

_── EXCLUSIONES (portado de exclReason / isExcludedEmp) ── Normaliza "Apellido, Nombre" para comparar sin importar espacios/mayusculas._

```js
function normEmp(name) {
```
<sub>lib/calc.js:8</sub>

_excludedList: array de nombres (de la tabla excluded_employees)_

```js
export function isExcludedEmp(name, excludedList) {
```
<sub>lib/calc.js:13</sub>

_Devuelve la razon de exclusion, o "" si no se excluye. jtN = job title normalizado (lowercase, sin asterisco final)_

```js
export function exclReason(empName, jtN, excludedList) {
```
<sub>lib/calc.js:20</sub>

_Normaliza el job title igual que SHIFT: lowercase, quita "*" final._

```js
export function normJobTitle(jt) {
```
<sub>lib/calc.js:29</sub>

```js
// ── TARGETS (portado de getTarget / ptdTarget) ──
```
<sub>lib/calc.js:33</sub>

_── TARGETS (portado de getTarget / ptdTarget) ──_

```js
// store = objeto de la tabla stores { weekday_target, weekend_target, ptd_target, grp }
```
<sub>lib/calc.js:34</sub>

_── TARGETS (portado de getTarget / ptdTarget) ── store = objeto de la tabla stores { weekday_target, weekend_target, ptd_target, grp }_

```js
export function getTarget(store, day) {
```
<sub>lib/calc.js:35</sub>

```js
export function getPtdTarget(store) {
```
<sub>lib/calc.js:40</sub>

_── AGREGACION DE HORAS DESDE PAYROLL (portado de parsePay) ──_

```js
// Formula: Hours = Regular Hours + Overtime Hours, excluyendo GM/NSO/lista negra.
```
<sub>lib/calc.js:46</sub>

_── AGREGACION DE HORAS DESDE PAYROLL (portado de parsePay) ── Formula: Hours = Regular Hours + Overtime Hours, excluyendo GM/NSO/lista negra._

```js
// Trainee y Certified Trainer se suman aparte (y tambien cuentan en las horas totales).
```
<sub>lib/calc.js:47</sub>

_── AGREGACION DE HORAS DESDE PAYROLL (portado de parsePay) ── Formula: Hours = Regular Hours + Overtime Hours, excluyendo GM/NSO/lista negra. Trainee y Certified Trainer se suman aparte (y tambien cuentan en las horas totales)._

```js
// rows: array de registros de payroll con { employee, jobTitle, regularHours, overtimeHours, code }
```
<sub>lib/calc.js:48</sub>

_Formula: Hours = Regular Hours + Overtime Hours, excluyendo GM/NSO/lista negra. Trainee y Certified Trainer se suman aparte (y tambien cuentan en las horas totales). rows: array de registros de payroll con { employee, jobTitle, regularHours, overtimeHours, code }_

```js
export function aggregatePayroll(rows, excludedList) {
```
<sub>lib/calc.js:49</sub>

```js
const total = regH + otH;
```
<sub>lib/calc.js:58</sub>

```js
if (total <= 0) return;
```
<sub>lib/calc.js:59</sub>

```js
hours[code] = (hours[code] || 0) + total;
```
<sub>lib/calc.js:63</sub>

```js
if (jtN === "trainee") trainee[code] = (trainee[code] || 0) + total;
```
<sub>lib/calc.js:64</sub>

```js
if (jtN === "certified trainer") trainer[code] = (trainer[code] || 0) + total;
```
<sub>lib/calc.js:65</sub>

```js
// Total training = trainee + trainer (portado exacto)
```
<sub>lib/calc.js:67</sub>

```js
Object.keys(trainee).forEach((k) => { train[k] = (train[k] || 0) + trainee[k]; });
```
<sub>lib/calc.js:69</sub>

```js
Object.keys(trainer).forEach((k) => { train[k] = (train[k] || 0) + trainer[k]; });
```
<sub>lib/calc.js:70</sub>

_── METRICAS POR DIA/TIENDA (portado de la logica del week view / email) ──_

```js
// sales, hours son numeros; store el objeto de tienda; day el nombre del dia.
```
<sub>lib/calc.js:75</sub>

_── METRICAS POR DIA/TIENDA (portado de la logica del week view / email) ── sales, hours son numeros; store el objeto de tienda; day el nombre del dia._

```js
export function dayMetrics(sales, hours, store, day) {
```
<sub>lib/calc.js:76</sub>

```js
const sp = hours > 0 ? sales / hours : 0;      // SPLH
```
<sub>lib/calc.js:78</sub>

```js
const ou = (sales / t) - hours;                 // Hours (Over)/Under
```
<sub>lib/calc.js:79</sub>

```js
const ok = sp >= t;                             // verde si cumple target
```
<sub>lib/calc.js:80</sub>

```js
// TPLH: transacciones (checks) por hora de labor. A diferencia de SPLH,
```
<sub>lib/calc.js:84</sub>

_TPLH: transacciones (checks) por hora de labor. A diferencia de SPLH,_

```js
// no se infla con aumentos de precio ni con dias de ticket promedio alto.
```
<sub>lib/calc.js:85</sub>

_TPLH: transacciones (checks) por hora de labor. A diferencia de SPLH, no se infla con aumentos de precio ni con dias de ticket promedio alto._

```js
// Mide throughput real por persona. transactions viene de daily_sales.transaction_count.
```
<sub>lib/calc.js:86</sub>

_TPLH: transacciones (checks) por hora de labor. A diferencia de SPLH, no se infla con aumentos de precio ni con dias de ticket promedio alto. Mide throughput real por persona. transactions viene de daily_sales.transaction_count._

```js
export function tplh(transactions, hours) {
```
<sub>lib/calc.js:87</sub>

```js
if (!hours || hours <= 0) return 0;
```
<sub>lib/calc.js:88</sub>

```js
return (transactions || 0) / hours;
```
<sub>lib/calc.js:89</sub>

_── DETECCION DE ANOMALIAS (portado exacto de anomalyFlags) ──_

```js
export function anomalyFlags(store, s, h, day) {
```
<sub>lib/calc.js:93</sub>

```js
if (h < 1 && s >= 1) flags.push("No hours (payroll missing?)");
```
<sub>lib/calc.js:96</sub>

```js
else if (s < 1 && h >= 1) flags.push("No sales (Location Overview missing?)");
```
<sub>lib/calc.js:97</sub>

```js
if (sp >= t * 2.5) flags.push("SPLH very high (" + Math.round(sp) + " vs target " + t + ")");
```
<sub>lib/calc.js:101</sub>

```js
if (h > 400) flags.push("Hours very high (" + Math.round(h) + "h in one day)");
```
<sub>lib/calc.js:103</sub>

_Formateo (Over)/Under igual que SHIFT: negativo -> "(N)", sin signo +_

```js
export function fmtOverUnder(n) {
```
<sub>lib/calc.js:108</sub>

---

## `lib/data.js`

_Todas estas funciones asumen que reciben un cliente supabaseAdmin ya inicializado. ── TIENDAS ──_

```js
export async function getAllStores(supabase) {
```
<sub>lib/data.js:5</sub>

```js
export async function updateStoreTargets(supabase, code, weekdayTarget, weekendTarget, ptdTarget) {
```
<sub>lib/data.js:18</sub>

```js
export async function addStore(supabase, { code, name, region, grp, weekdayTarget, weekendTarget, ptdTarget }) {
```
<sub>lib/data.js:30</sub>

```js
export async function renameStore(supabase, code, newName) {
```
<sub>lib/data.js:41</sub>

```js
export async function removeStore(supabase, code) {
```
<sub>lib/data.js:46</sub>

```js
// ── LABOR (horas ya agregadas por tienda/dia, viene de toast_labor_shifts) ──
```
<sub>lib/data.js:55</sub>

_── LABOR (horas ya agregadas por tienda/dia, viene de toast_labor_shifts) ──_

```js
// Devuelve { [code]: { hours, trainee, trainer, train } } para una fecha dada.
```
<sub>lib/data.js:56</sub>

_── LABOR (horas ya agregadas por tienda/dia, viene de toast_labor_shifts) ── Devuelve { [code]: { hours, trainee, trainer, train } } para una fecha dada._

```js
export async function getLaborForDate(supabase, businessDate, excludedList) {
```
<sub>lib/data.js:57</sub>

```js
regularHours: r.hours, // Toast ya da el total; ajustar si separan reg/OT
```
<sub>lib/data.js:69</sub>

```js
// ── VENTAS (de daily_sales, llenada por el sync de Toast) ──
```
<sub>lib/data.js:76</sub>

_── VENTAS (de daily_sales, llenada por el sync de Toast) ──_

```js
export async function getSalesForDate(supabase, businessDate) {
```
<sub>lib/data.js:77</sub>

```js
export async function upsertDailySales(supabase, businessDate, salesMap) {
```
<sub>lib/data.js:88</sub>

_── EXCLUSIONES ──_

```js
export async function getExcludedEmployees(supabase) {
```
<sub>lib/data.js:102</sub>

```js
export async function addExcludedEmployee(supabase, name, reason) {
```
<sub>lib/data.js:108</sub>

```js
export async function removeExcludedEmployee(supabase, name) {
```
<sub>lib/data.js:113</sub>

_── CHANGELOG ──_

```js
export async function logChange(supabase, action, detail) {
```
<sub>lib/data.js:119</sub>

---

## `lib/email-generator.js`

```js
const G_BG = "#C6EFCE", G_TX = "#276221"; // SPLH verde
```
<sub>lib/email-generator.js:11</sub>

```js
const R_BG = "#FFC7CE", R_TX = "#9C0006"; // SPLH rojo
```
<sub>lib/email-generator.js:12</sub>

```js
const FH = FM + "font-size:11pt;font-weight:700;";
```
<sub>lib/email-generator.js:16</sub>

```js
function td(extra) { return `style="${FD}border:${BORDER};padding:2px 6px;vertical-align:middle;${extra}"`; }
```
<sub>lib/email-generator.js:18</sub>

```js
function tdR(extra) { return td("text-align:right;white-space:nowrap;" + extra); }
```
<sub>lib/email-generator.js:19</sub>

```js
function thCol(txt, extra) {
```
<sub>lib/email-generator.js:20</sub>

_- isFullWeek: bool - hasPTD: bool /_

```js
export function generateEmailHTML({ day, weekNumber, refDate, groupedStores, isFullWeek, hasPTD, acNote }) {
```
<sub>lib/email-generator.js:33</sub>

```js
+ `<td ${tdR("")}>${Math.round(d.hours)}</td>`
```
<sub>lib/email-generator.js:45</sub>

```js
+ `<td ${tdR("")}>${d.sales > 0 ? "$" + Math.round(d.sales).toLocaleString("en-US") : "$0"}</td>`
```
<sub>lib/email-generator.js:46</sub>

```js
+ `<td ${tdR("")}>$${d.target}</td>`
```
<sub>lib/email-generator.js:47</sub>

```js
+ `<td ${tdR("")}>${fmtOverUnder(d.overUnder)}</td>`
```
<sub>lib/email-generator.js:49</sub>

```js
+ `<td ${tdR("border-left:2px solid #999;")}>${Math.round(w.hours)}</td>`
```
<sub>lib/email-generator.js:50</sub>

```js
+ `<td ${tdR("")}>${w.sales > 0 ? "$" + Math.round(w.sales).toLocaleString("en-US") : "$0"}</td>`
```
<sub>lib/email-generator.js:51</sub>

```js
+ `<td ${tdR("")}>${fmtOverUnder(w.overUnder)}</td>`
```
<sub>lib/email-generator.js:53</sub>

```js
+ `<td ${tdR("border-left:2px solid #999;")}>${Math.round(w.trainTotal) > 0 ? Math.round(w.trainTotal) : "-"}</td>`
```
<sub>lib/email-generator.js:54</sub>

```js
+ `<td ${tdR("")}>${Math.round(w.trainee) > 0 ? Math.round(w.trainee) : "-"}</td>`
```
<sub>lib/email-generator.js:55</sub>

```js
+ `<td ${tdR("")}>${Math.round(w.trainer) > 0 ? Math.round(w.trainer) : "-"}</td>`;
```
<sub>lib/email-generator.js:56</sub>

```js
+ thCol("Hours Worked") + thCol("Gross Sales") + thCol("Target") + thCol("SPLH") + thCol("Hours (Over) /<br>Under")
```
<sub>lib/email-generator.js:83</sub>

```js
+ thCol("WTD Hours<br>Worked", "border-left:2px solid #999;") + thCol("WTD Gross<br>Sales") + thCol("WTD SPLH") + thCol("Hours (Over) /<br>Under")
```
<sub>lib/email-generator.js:84</sub>

```js
+ thCol("Total Training<br>Hours", "border-left:2px solid #999;") + thCol("Trainee<br>Hours") + thCol("Trainer<br>Hours")
```
<sub>lib/email-generator.js:85</sub>

```js
+ (hasPTD ? thCol("PTD Hours<br>Worked", "border-left:2px solid #999;") + thCol("PTD Gross<br>Sales") + thCol("PTD SPLH") : "")
```
<sub>lib/email-generator.js:86</sub>

---

## `lib/excel-export.js`

_Los numeros van CRUDOS para que Excel los trate como numeros y el equipo pueda sumar, ordenar y hacer pivotes._

```js
const DAY_COLS = 5;   // Hours, Sales, Target, SPLH, (Over)/Under
```
<sub>lib/excel-export.js:13</sub>

```js
const WTD_COLS = 4;   // Hours, Sales, SPLH, (Over)/Under
```
<sub>lib/excel-export.js:14</sub>

```js
const TRN_COLS = 3;   // Total, Trainee, Trainer
```
<sub>lib/excel-export.js:15</sub>

```js
const PTD_COLS = 3;   // Hours, Sales, SPLH
```
<sub>lib/excel-export.js:16</sub>

```js
.tot      { background: #eef1f4; font-weight: bold; }
```
<sub>lib/excel-export.js:40</sub>

```js
function cell(cls, value) {
```
<sub>lib/excel-export.js:47</sub>

_days: [{ iso, dayName, byStore: { [code]: dayData } }] en orden Lun..Dom_

```js
// groupedStores: filas del ultimo dia con data, con wtd y ptd de la semana
```
<sub>lib/excel-export.js:52</sub>

_days: [{ iso, dayName, byStore: { [code]: dayData } }] en orden Lun..Dom groupedStores: filas del ultimo dia con data, con wtd y ptd de la semana_

```js
export function generateWeekExcel({ weekNumber, period, weekStart, weekEnd, days, groupedStores }) {
```
<sub>lib/excel-export.js:53</sub>

```js
const TOTAL_COLS = 1 + days.length * DAY_COLS + WTD_COLS + TRN_COLS + PTD_COLS;
```
<sub>lib/excel-export.js:54</sub>

```js
// ---- banda superior ----
```
<sub>lib/excel-export.js:56</sub>

```js
bandRow += `<td class="band${i === 0 ? " sep" : ""}" colspan="${DAY_COLS}">${label}</td>`;
```
<sub>lib/excel-export.js:60</sub>

```js
bandRow += `<td class="band sep" colspan="${WTD_COLS}">Week to Date</td>`;
```
<sub>lib/excel-export.js:62</sub>

```js
bandRow += `<td class="band sep" colspan="${TRN_COLS}">Training</td>`;
```
<sub>lib/excel-export.js:63</sub>

```js
bandRow += `<td class="band sep" colspan="${PTD_COLS}">Period to Date</td>`;
```
<sub>lib/excel-export.js:64</sub>

```js
headRow += `<td class="hdr${i === 0 ? " sep" : ""}">Hours</td>`
```
<sub>lib/excel-export.js:69</sub>

```js
+ `<td class="hdr">Gross Sales</td>`
```
<sub>lib/excel-export.js:70</sub>

```js
+ `<td class="hdr">Target</td>`
```
<sub>lib/excel-export.js:71</sub>

```js
+ `<td class="hdr">SPLH</td>`
```
<sub>lib/excel-export.js:72</sub>

```js
headRow += `<td class="hdr sep">WTD<br>Hours</td><td class="hdr">WTD<br>Gross Sales</td>`
```
<sub>lib/excel-export.js:75</sub>

```js
+ `<td class="hdr">WTD<br>SPLH</td><td class="hdr">WTD (Over)/<br>Under</td>`
```
<sub>lib/excel-export.js:76</sub>

```js
+ `<td class="hdr sep">Total<br>Training</td><td class="hdr">Trainee</td><td class="hdr">Trainer</td>`
```
<sub>lib/excel-export.js:77</sub>

```js
+ `<td class="hdr sep">PTD<br>Hours</td><td class="hdr">PTD<br>Gross Sales</td><td class="hdr">PTD<br>SPLH</td>`;
```
<sub>lib/excel-export.js:78</sub>

```js
rows += `<tr><td class="reg" colspan="${TOTAL_COLS}">${regObj.label}</td></tr>`;
```
<sub>lib/excel-export.js:86</sub>

```js
r += cell("num" + sep, Math.round(dd.hours))
```
<sub>lib/excel-export.js:98</sub>

```js
+ cell("money", Math.round(dd.sales))
```
<sub>lib/excel-export.js:99</sub>

```js
+ cell("tgt", dd.target)
```
<sub>lib/excel-export.js:100</sub>

```js
+ cell(dd.ok ? "ok" : "bad", dd.splh)
```
<sub>lib/excel-export.js:101</sub>

```js
+ cell("ou", dd.overUnder);
```
<sub>lib/excel-export.js:102</sub>

```js
r += cell("num tot sep", Math.round(w.hours))
```
<sub>lib/excel-export.js:107</sub>

```js
+ cell("money tot", Math.round(w.sales))
```
<sub>lib/excel-export.js:108</sub>

```js
+ cell(w.ok ? "ok" : "bad", w.splh)
```
<sub>lib/excel-export.js:109</sub>

```js
+ cell("ou tot", w.overUnder);
```
<sub>lib/excel-export.js:110</sub>

```js
r += cell("num sep", w.trainTotal || 0)
```
<sub>lib/excel-export.js:112</sub>

```js
+ cell("num", w.trainee || 0)
```
<sub>lib/excel-export.js:113</sub>

```js
+ cell("num", w.trainer || 0);
```
<sub>lib/excel-export.js:114</sub>

```js
r += cell("num tot sep", Math.round(p.hours))
```
<sub>lib/excel-export.js:120</sub>

```js
+ cell("money tot", Math.round(p.sales))
```
<sub>lib/excel-export.js:121</sub>

```js
+ cell(p.ok ? "ok" : "bad", p.splh);
```
<sub>lib/excel-export.js:122</sub>

```js
+ `<tr><td class="title" colspan="${TOTAL_COLS}">SHIFT Labor Dashboard &mdash; Week ${weekNumber}, Period ${period}</td></tr>`
```
<sub>lib/excel-export.js:144</sub>

```js
+ `<tr><td class="subtitle" colspan="${TOTAL_COLS}">${weekStart} through ${weekEnd}</td></tr>`
```
<sub>lib/excel-export.js:145</sub>

```js
+ `<tr>${bandRow}</tr><tr>${headRow}</tr>${rows}`
```
<sub>lib/excel-export.js:146</sub>

---

## `lib/fiscal.js`

_Period 05 used to end on 2026-05-03, which left the three weeks from May 4 to May 24 belonging to no period at all. getPeriodForDate returned_

```js
// null for any date in that gap, which silently broke PTD totals and the
```
<sub>lib/fiscal.js:5</sub>

_Period 05 used to end on 2026-05-03, which left the three weeks from May 4 to May 24 belonging to no period at all. getPeriodForDate returned null for any date in that gap, which silently broke PTD totals and the_

```js
// review rollup. The end date now meets the start of period 06, and every
```
<sub>lib/fiscal.js:6</sub>

_nothing here checks whether 2027 is one of those years. Confirm these dates against the real published calendar before period 2027-01 opens (2026-12-28), the same way 2026's gap above had to be caught by hand._

```js
export const FISCAL_PERIODS = {
```
<sub>lib/fiscal.js:21</sub>

```js
export function getPeriodForWeek(wk) {
```
<sub>lib/fiscal.js:48</sub>

_Numero de semana ISO — portado de getWN() en SHIFT_

```js
export function getWeekNumber(date = new Date()) {
```
<sub>lib/fiscal.js:57</sub>

```js
export const DAYS = ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday"];
```
<sub>lib/fiscal.js:65</sub>

```js
export const WEEKEND = new Set(["Friday", "Saturday", "Sunday"]);
```
<sub>lib/fiscal.js:66</sub>

```js
export function getWeekStart(date = new Date()) {
```
<sub>lib/fiscal.js:68</sub>

```js
export function getPeriodForDate(date = new Date()) {
```
<sub>lib/fiscal.js:75</sub>

_Every period, oldest first. Used by any view that needs to lay periods out side by side, the way the manual Tattle Report did with P4 through P7. /_

```js
export function listPeriods() {
```
<sub>lib/fiscal.js:91</sub>

_Fails loudly, at import time, the moment today falls in a gap FISCAL_PERIODS_

```js
// doesn't cover - instead of every PTD column on the dashboard and in the
```
<sub>lib/fiscal.js:98</sub>

```js
"). Every PTD figure on the dashboard and in the daily email will read " +
```
<sub>lib/fiscal.js:107</sub>

---

## `lib/forecast.js`

```js
function dayNameFromISO(iso) {
```
<sub>lib/forecast.js:14</sub>

```js
function addDays(iso, n) {
```
<sub>lib/forecast.js:19</sub>

```js
function isoRange(startIso, endIso) {
```
<sub>lib/forecast.js:25</sub>

```js
function todayCentral() {
```
<sub>lib/forecast.js:36</sub>

_Lunes de la semana que contiene esta fecha_

```js
export function mondayOf(iso) {
```
<sub>lib/forecast.js:43</sub>

_Confianza basada en dispersion. Un GM debe saber cuando el pronostico es solido y cuando es una adivinanza._

```js
function confidenceOf(values) {
```
<sub>lib/forecast.js:52</sub>

```js
if (mean <= 0) return { level: "low", label: "No sales history" };
```
<sub>lib/forecast.js:55</sub>

```js
const variance = values.reduce((a, b) => a + Math.pow(b - mean, 2), 0) / values.length;
```
<sub>lib/forecast.js:56</sub>

```js
const cv = Math.sqrt(variance) / mean;
```
<sub>lib/forecast.js:57</sub>

```js
export async function buildForecast(storeCode, weekStartIso, lookbackWeeks = 4) {
```
<sub>lib/forecast.js:63</sub>

```js
if (salesErr) throw new Error("sales: " + salesErr.message);
```
<sub>lib/forecast.js:99</sub>

```js
const minSales = values.length ? Math.min(...values) : 0;
```
<sub>lib/forecast.js:137</sub>

```js
const maxSales = values.length ? Math.max(...values) : 0;
```
<sub>lib/forecast.js:138</sub>

```js
const allowed = target > 0 ? expected / target : 0;
```
<sub>lib/forecast.js:141</sub>

```js
const allowedLow = target > 0 ? minSales / target : 0;
```
<sub>lib/forecast.js:142</sub>

```js
const allowedHigh = target > 0 ? maxSales / target : 0;
```
<sub>lib/forecast.js:143</sub>

```js
expectedSales: Math.round(expected),
```
<sub>lib/forecast.js:153</sub>

```js
minSales: Math.round(minSales),
```
<sub>lib/forecast.js:154</sub>

```js
maxSales: Math.round(maxSales),
```
<sub>lib/forecast.js:155</sub>

```js
allowedHours: Math.round(allowed),
```
<sub>lib/forecast.js:156</sub>

```js
variance: hasPlan ? Math.round(planned - allowed) : null,
```
<sub>lib/forecast.js:160</sub>

```js
const totalExpected = withForecast.reduce((a, d) => a + d.expectedSales, 0);
```
<sub>lib/forecast.js:170</sub>

```js
const totalAllowed = withForecast.reduce((a, d) => a + d.allowedHours, 0);
```
<sub>lib/forecast.js:171</sub>

```js
const totalPlanned = planned.reduce((a, d) => a + d.plannedHours, 0);
```
<sub>lib/forecast.js:173</sub>

```js
detail: `Expected sales of $${totalExpected.toLocaleString("en-US")} support up to about ${totalAllowed} hours. Enter your plan below to compare.`,
```
<sub>lib/forecast.js:181</sub>

```js
const diff = Math.round(totalPlanned - totalAllowed);
```
<sub>lib/forecast.js:184</sub>

```js
const pct = totalAllowed > 0 ? Math.abs(diff) / totalAllowed : 0;
```
<sub>lib/forecast.js:185</sub>

```js
detail: `You planned ${Math.round(totalPlanned)} hours, but expected sales of $${totalExpected.toLocaleString("en-US")} only support about ${totalAllowed}. Cutting ${diff} hours keeps you at target.`,
```
<sub>lib/forecast.js:190</sub>

```js
headline: `Your schedule is ${Math.abs(diff)} hours under the ceiling`,
```
<sub>lib/forecast.js:195</sub>

```js
detail: `You planned ${Math.round(totalPlanned)} hours against a ceiling of about ${totalAllowed}. Staying under it is how you beat target, so this is only a problem if you are short on coverage.`,
```
<sub>lib/forecast.js:196</sub>

```js
detail: `${Math.round(totalPlanned)} hours planned against about ${totalAllowed} supported by expected sales of $${totalExpected.toLocaleString("en-US")}.`,
```
<sub>lib/forecast.js:202</sub>

```js
plannedHours: daysPlanned ? Math.round(totalPlanned) : null,
```
<sub>lib/forecast.js:219</sub>

```js
variance: daysPlanned ? Math.round(totalPlanned - totalAllowed) : null,
```
<sub>lib/forecast.js:220</sub>

---

## `lib/hme.js`

_HME caps a single request window at 72h. Ask for slightly less so that clock skew never trips the 400 "dates_is_out_of_range"._

```js
export const WINDOW_HOURS = 70;
```
<sub>lib/hme.js:8</sub>

_Cloud data runs at least 15 min behind. Never request the last 20._

```js
export const LAG_MINUTES = 20;
```
<sub>lib/hme.js:11</sub>

```js
export function nextLimit() {
```
<sub>lib/hme.js:16</sub>

```js
function headers() {
```
<sub>lib/hme.js:22</sub>

```js
export async function hmeCall(path, params = {}, attempt = 1) {
```
<sub>lib/hme.js:31</sub>

```js
export function listHmeStores() {
```
<sub>lib/hme.js:55</sub>

_and that is exactly what caused the CA store date misattribution in the labor sync. /_

```js
export function localDateParts(iso) {
```
<sub>lib/hme.js:102</sub>

_Flatten one HME car record into 0..n rows for hme_car_events._

```js
export function flattenRecord(rec, storeCode) {
```
<sub>lib/hme.js:120</sub>

_Flatten a full RCD endpoint response._

```js
export function flattenResponse(body, storeCode) {
```
<sub>lib/hme.js:169</sub>

_Fetch one window (<= 70h) for one store, paging through Offset. Returns rows ready to upsert into hme_car_events. /_

```js
export async function fetchWindow(hmeStoreNumber, storeCode, startIso, endIso) {
```
<sub>lib/hme.js:186</sub>

---

## `lib/hourly-shape.js`

_ Por construccion la curva suma exactamente las horas que el header ya muestra. Ese es el punto. Si esto reimplementara la seleccion de fechas_

```js
// o el promedio, tarde o temprano se separaria de buildForecast y el GM
```
<sub>lib/hourly-shape.js:11</sub>

_o el promedio, tarde o temprano se separaria de buildForecast y el GM veria 76 arriba y 74 abajo, y ese dia deja de usar la pantalla._

```js
// Por eso llamamos a buildForecast y consumimos su sampleDates y su
```
<sub>lib/hourly-shape.js:14</sub>

_veria 76 arriba y 74 abajo, y ese dia deja de usar la pantalla.  Por eso llamamos a buildForecast y consumimos su sampleDates y su_

```js
// allowedHours en vez de recalcularlos.
```
<sub>lib/hourly-shape.js:15</sub>

_Supabase corta en 1000 filas por consulta. Con lookback 12 son 84 fechas x 24 horas = 2016 filas, y la mitad se perderia en silencio. Ya nos paso_

```js
// una vez con la paginacion de daily_sales, no se repite.
```
<sub>lib/hourly-shape.js:35</sub>

```js
function hourLabel(hour) {
```
<sub>lib/hourly-shape.js:38</sub>

```js
// Reparte un entero total entre pesos usando restos mayores, para que la
```
<sub>lib/hourly-shape.js:44</sub>

_Reparte un entero total entre pesos usando restos mayores, para que la_

```js
// suma de los enteros sea EXACTAMENTE el total. Redondear cada hora por
```
<sub>lib/hourly-shape.js:45</sub>

_Reparte un entero total entre pesos usando restos mayores, para que la suma de los enteros sea EXACTAMENTE el total. Redondear cada hora por separado daria 74 u 78 donde el header dice 76._

```js
function distributeWholeHours(weights, total) {
```
<sub>lib/hourly-shape.js:47</sub>

```js
const target = Math.round(total);
```
<sub>lib/hourly-shape.js:49</sub>

```js
const sum = weights.reduce((a, b) => a + b, 0);
```
<sub>lib/hourly-shape.js:52</sub>

```js
let left = target - out.reduce((a, b) => a + b, 0);
```
<sub>lib/hourly-shape.js:58</sub>

```js
.sort((a, b) => b.frac - a.frac || b.weight - a.weight);
```
<sub>lib/hourly-shape.js:65</sub>

```js
// La banda vive aqui y no en el componente para que haya una sola verdad.
```
<sub>lib/hourly-shape.js:77</sub>

_La banda vive aqui y no en el componente para que haya una sola verdad. Si maniana agregamos el reporte por correo, usa estos mismos cortes._

```js
function bandOf(staff, peak) {
```
<sub>lib/hourly-shape.js:79</sub>

_Mejor bloque contiguo de `size` horas. Se reporta como ventana y no como hora suelta porque un turno se mueve en bloques, no en instantes._

```js
function bestWindow(staff, size) {
```
<sub>lib/hourly-shape.js:89</sub>

```js
for (let h = 0; h + size <= HOURS_IN_DAY; h++) {
```
<sub>lib/hourly-shape.js:91</sub>

_Primera hora despues del pico en la que el dia baja a tranquilo y ya no vuelve a subir. Es la que le dice al gerente donde puede empezar a cortar._

```js
function calmsDownAt(bands, peakHour) {
```
<sub>lib/hourly-shape.js:108</sub>

```js
for (let h = peakHour + 1; h < HOURS_IN_DAY; h++) {
```
<sub>lib/hourly-shape.js:110</sub>

```js
for (let k = h + 1; k < HOURS_IN_DAY; k++) {
```
<sub>lib/hourly-shape.js:113</sub>

```js
async function fetchHourlyRows(storeCode, dates) {
```
<sub>lib/hourly-shape.js:121</sub>

```js
if (error) throw new Error("hourly_sales: " + error.message);
```
<sub>lib/hourly-shape.js:145</sub>

```js
export async function buildHourlyPlan(storeCode, weekStartIso, lookbackWeeks = 4) {
```
<sub>lib/hourly-shape.js:157</sub>

```js
if (!(h >= 0 && h < HOURS_IN_DAY)) return;
```
<sub>lib/hourly-shape.js:187</sub>

```js
salesByDate[r.business_date][h] += Number(r.gross_sales) || 0;
```
<sub>lib/hourly-shape.js:192</sub>

```js
txnsByDate[r.business_date][h] += Number(r.transaction_count) || 0;
```
<sub>lib/hourly-shape.js:193</sub>

```js
salesByDate[iso].forEach((v, h) => { avgSales[h] += v; });
```
<sub>lib/hourly-shape.js:226</sub>

```js
txnsByDate[iso].forEach((v, h) => { avgTxns[h] += v; });
```
<sub>lib/hourly-shape.js:227</sub>

```js
for (let h = 0; h < HOURS_IN_DAY; h++) {
```
<sub>lib/hourly-shape.js:229</sub>

```js
avgSales[h] = avgSales[h] / covered;
```
<sub>lib/hourly-shape.js:230</sub>

```js
avgTxns[h] = avgTxns[h] / covered;
```
<sub>lib/hourly-shape.js:231</sub>

```js
const dayTotal = avgSales.reduce((a, b) => a + b, 0);
```
<sub>lib/hourly-shape.js:234</sub>

```js
const share = sales / dayTotal;
```
<sub>lib/hourly-shape.js:245</sub>

```js
endLabel: hourLabel((h + 1) % HOURS_IN_DAY),
```
<sub>lib/hourly-shape.js:253</sub>

```js
expectedSales: Math.round(day.expectedSales * share),
```
<sub>lib/hourly-shape.js:254</sub>

```js
expectedTransactions: Math.round(avgTxns[h]),
```
<sub>lib/hourly-shape.js:255</sub>

---

## `lib/leaderboard.js`

_everywhere else rankableRating is used directly (e.g. the Leaderboard's own Reviews board, which still lets someone look at the raw week number). /_

```js
export const MIN_REVIEWS_TO_RANK = 5;
```
<sub>lib/leaderboard.js:20</sub>

_constant, not a per-store column: every store is held to the same bar on guest experience, unlike SPLH where each store has its own number. /_

```js
export const RATING_TARGET = 4.5;
```
<sub>lib/leaderboard.js:28</sub>

```js
export const WEIGHTS = { efficiency: 0.5, reviews: 0.5 };
```
<sub>lib/leaderboard.js:30</sub>

_efficiency board could disagree about which number they were reading. One function now, both callers. /_

```js
export function splhOf(s) {
```
<sub>lib/leaderboard.js:40</sub>

```js
/** The store's own SPLH target. */
```
<sub>lib/leaderboard.js:44</sub>

_The store's own SPLH target._

```js
export function targetOf(s) {
```
<sub>lib/leaderboard.js:45</sub>

_the target allowed. Measuring each store against its own number is what lets a $75 target store and a $90 target store compete fairly. /_

```js
export function efficiencyOf(s) {
```
<sub>lib/leaderboard.js:55</sub>

_Explicit null checks, not falsy checks: a store with a genuinely valid_

```js
// splh of 0 (no trade yet this week, or closed all week) used to be
```
<sub>lib/leaderboard.js:59</sub>

_Explicit null checks, not falsy checks: a store with a genuinely valid splh of 0 (no trade yet this week, or closed all week) used to be treated the same as "no data" and silently dropped from every board,_

```js
// including the Reviews-only one, which never even reads this field.
```
<sub>lib/leaderboard.js:61</sub>

```js
if (!target) return null; // a zero target is a data error, never divide by it
```
<sub>lib/leaderboard.js:63</sub>

```js
return (splh / target) * 100;
```
<sub>lib/leaderboard.js:64</sub>

_Anything under MIN_REVIEWS_TO_RANK is null rather than a low number, so a store with two reviews is never treated as if it had earned that average. /_

```js
export function rankableRating(s) {
```
<sub>lib/leaderboard.js:73</sub>

```js
return rev.count >= MIN_REVIEWS_TO_RANK ? rev.rating : null;
```
<sub>lib/leaderboard.js:76</sub>

_because someone flips that toggle. MIN_REVIEWS_TO_RANK is still applied, against the period count, via the same rankableRating logic. /_

```js
export function ratingComponentOf(s) {
```
<sub>lib/leaderboard.js:99</sub>

```js
// Explicit null check, not a falsy check: rankableRating already returns
```
<sub>lib/leaderboard.js:101</sub>

_Explicit null check, not a falsy check: rankableRating already returns_

```js
// null for "not enough reviews yet," and that must stay null here too,
```
<sub>lib/leaderboard.js:102</sub>

_Explicit null check, not a falsy check: rankableRating already returns null for "not enough reviews yet," and that must stay null here too,_

```js
// never be coerced into a rating of 0.
```
<sub>lib/leaderboard.js:103</sub>

```js
return (rating / RATING_TARGET) * 100;
```
<sub>lib/leaderboard.js:105</sub>

_through this — see ratingComponentOf, which is scored against RATING_TARGET instead of the field. /_

```js
export function percentiles(values) {
```
<sub>lib/leaderboard.js:118</sub>

```js
const pct = (((i + j) / 2) / last) * 100;
```
<sub>lib/leaderboard.js:137</sub>

```js
for (let k = i; k <= j; k++) out[sorted[k].i] = pct;
```
<sub>lib/leaderboard.js:138</sub>

_other column has to build its own order, because a rank that does not match the order the rows are printed in is worse than no rank at all. /_

```js
export function scoreStores(reportRows, windowKey = "period") {
```
<sub>lib/leaderboard.js:158</sub>

```js
// A store without enough reviews is ranked on labor alone rather than
```
<sub>lib/leaderboard.js:176</sub>

```js
? e * WEIGHTS.efficiency + r * WEIGHTS.reviews
```
<sub>lib/leaderboard.js:180</sub>

```js
score: score === null ? null : Math.round(score * 10) / 10,
```
<sub>lib/leaderboard.js:186</sub>

```js
const byScore = [...rows].sort((a, b) => (b.score ?? -1) - (a.score ?? -1));
```
<sub>lib/leaderboard.js:191</sub>

```js
byScore.forEach((s, i) => (rank[s.code] = i + 1));
```
<sub>lib/leaderboard.js:193</sub>

_should not move the company number as much as one with sixty, so the average is weighted by review count rather than by store. /_

```js
export function weightedRating(reportRows, windowKey = "period") {
```
<sub>lib/leaderboard.js:203</sub>

```js
sum += rev.rating * rev.count;
```
<sub>lib/leaderboard.js:210</sub>

```js
return n > 0 ? { rating: sum / n, count: n } : { rating: null, count: 0 };
```
<sub>lib/leaderboard.js:213</sub>

---

## `lib/permissions.js`

```js
export const ROLE_ADMIN = "admin";
```
<sub>lib/permissions.js:15</sub>

```js
export const ROLE_REGIONAL = "regional";
```
<sub>lib/permissions.js:16</sub>

```js
export const ROLE_NONE = "none";
```
<sub>lib/permissions.js:17</sub>

```js
export const GRP_TX_TN = "TX-TN";
```
<sub>lib/permissions.js:19</sub>

```js
export const GRP_CA_AZ = "CA-AZ";
```
<sub>lib/permissions.js:20</sub>

```js
export function resolveAccess(groups) {
```
<sub>lib/permissions.js:22</sub>

_Fail closed. An unknown role, a missing grp, or a store row that somehow has no grp at all, all resolve to no access. /_

```js
export function canSeeGrp(access, grp) {
```
<sub>lib/permissions.js:44</sub>

_shapes them. Keeping the filter in one place is the point: the same call is used by every route handler instead of each one reimplementing it. /_

```js
export function filterStores(access, rows) {
```
<sub>lib/permissions.js:58</sub>

---

## `lib/report.js`

```js
// Arma el reporte diario completo (Dia + WTD + PTD) por tienda, leyendo de Supabase.
```
<sub>lib/report.js:1</sub>

```js
function dayNameFromISO(iso) {
```
<sub>lib/report.js:11</sub>

```js
function addDays(iso, n) {
```
<sub>lib/report.js:16</sub>

```js
function isoRange(startIso, endIso) {
```
<sub>lib/report.js:22</sub>

_Fecha local (YYYY-MM-DD) de un instante UTC en la zona de la tienda. Necesario porque un turno de 5 PM en California es el dia siguiente en UTC._

```js
function localDateInTz(utcIso, tz) {
```
<sub>lib/report.js:35</sub>

```js
async function fetchAllRows(buildQuery, label) {
```
<sub>lib/report.js:46</sub>

```js
function normName(name) {
```
<sub>lib/report.js:61</sub>

_Horas de un turno. Si sigue abierto, cuenta el tiempo transcurrido, porque Toast no reporta horas hasta el clock-out. El tope de 18h evita_

```js
// que un turno viejo sin cerrar (error de data) infle el total.
```
<sub>lib/report.js:67</sub>

_Horas de un turno. Si sigue abierto, cuenta el tiempo transcurrido, porque Toast no reporta horas hasta el clock-out. El tope de 18h evita que un turno viejo sin cerrar (error de data) infle el total._

```js
function shiftHours(row) {
```
<sub>lib/report.js:68</sub>

```js
async function getExcludedEmployees() {
```
<sub>lib/report.js:76</sub>

```js
function exclusionReason(empName, jobTitle, excludedSet) {
```
<sub>lib/report.js:84</sub>

_onlyStore es opcional. Sin el, el comportamiento es identico al de antes._

```js
async function readLabor(startIso, endIso, excludedSet, tzByStore, onlyStore) {
```
<sub>lib/report.js:93</sub>

```js
hours[code] = (hours[code] || 0) + h;
```
<sub>lib/report.js:129</sub>

```js
if (jt === "trainee") trainee[code] = (trainee[code] || 0) + h;
```
<sub>lib/report.js:134</sub>

```js
if (jt === "certified trainer") trainer[code] = (trainer[code] || 0) + h;
```
<sub>lib/report.js:135</sub>

_onlyStore es opcional. Sin el, el comportamiento es identico al de antes._

```js
async function sumSalesByStore(startIso, endIso, onlyStore) {
```
<sub>lib/report.js:142</sub>

```js
map[r.store_code] = (map[r.store_code] || 0) + (r.gross_sales || 0);
```
<sub>lib/report.js:161</sub>

```js
perDay[r.store_code][r.business_date] = (perDay[r.store_code][r.business_date] || 0) + (r.gross_sales || 0);
```
<sub>lib/report.js:163</sub>

_Ajustes manuales. Viven en tabla aparte porque el sync sobrescribe_

```js
// toast_labor_shifts, y un ajuste guardado ahi se borraria en la
```
<sub>lib/report.js:169</sub>

_Ajustes manuales. Viven en tabla aparte porque el sync sobrescribe toast_labor_shifts, y un ajuste guardado ahi se borraria en la siguiente corrida. Se aplican al leer, no al escribir._

```js
async function readAdjustments(startIso, endIso) {
```
<sub>lib/report.js:171</sub>

```js
// map[code][date][field] = delta acumulado
```
<sub>lib/report.js:179</sub>

```js
map[code][date][a.field] = (map[code][date][a.field] || 0) + Number(a.delta);
```
<sub>lib/report.js:187</sub>

```js
function adjTotal(adjMap, code, field) {
```
<sub>lib/report.js:193</sub>

```js
function adjForDate(adjMap, code, date, field) {
```
<sub>lib/report.js:201</sub>

```js
function hasAdj(adjMap, code) {
```
<sub>lib/report.js:207</sub>

_having no reviews, with no error to say why. / Returns { map, failed }. On an RPC failure, map is {} (every store reads as_

```js
// having no reviews, same as before) but failed is true, so callers can tell
```
<sub>lib/report.js:224</sub>

_/ Returns { map, failed }. On an RPC failure, map is {} (every store reads as having no reviews, same as before) but failed is true, so callers can tell_

```js
// "genuinely no reviews" apart from "the rollup is broken and we don't know" -
```
<sub>lib/report.js:225</sub>

_having no reviews, same as before) but failed is true, so callers can tell "genuinely no reviews" apart from "the rollup is broken and we don't know" - those used to look identical outside a server log nobody watches._

```js
async function readReviews(startIso, endIso) {
```
<sub>lib/report.js:227</sub>

_Momento real del ultimo sync para esta fecha. Distinto de generatedAt, que es cuando se armo la respuesta (o sea, cuando cargo la pagina)._

```js
async function getLastSyncTime(isoDate) {
```
<sub>lib/report.js:261</sub>

_store in a new or renamed region could update one and not the other, and silently vanish from either the Week View/Leaderboard or the emailed/Excel report depending on which copy got missed._

```js
export function groupStoresForEmail(rows) {
```
<sub>lib/report.js:278</sub>

```js
export async function buildDailyReport(isoDate) {
```
<sub>lib/report.js:290</sub>

```js
if (pErr) throw new Error("ptd: " + pErr.message);
```
<sub>lib/report.js:342</sub>

_Dia, con ajustes_

```js
const dH = Math.max(0, (dayHours[code] || 0) + adjTotal(dayAdj.map, code, "hours"));
```
<sub>lib/report.js:379</sub>

```js
const dS = Math.max(0, (daySales[code] || 0) + adjTotal(dayAdj.map, code, "sales"));
```
<sub>lib/report.js:380</sub>

```js
const dSplh = dH > 0 ? dS / dH : 0;
```
<sub>lib/report.js:382</sub>

```js
const dOverUnder = dTarget > 0 ? (dS / dTarget) - dH : 0;
```
<sub>lib/report.js:383</sub>

```js
// WTD, con ajustes
```
<sub>lib/report.js:386</sub>

_WTD, con ajustes_

```js
const wH = Math.max(0, (wtdHours[code] || 0) + adjTotal(wtdAdj.map, code, "hours"));
```
<sub>lib/report.js:387</sub>

```js
const wS = Math.max(0, (wtdSales[code] || 0) + adjTotal(wtdAdj.map, code, "sales"));
```
<sub>lib/report.js:388</sub>

```js
const wSplh = wH > 0 ? wS / wH : 0;
```
<sub>lib/report.js:389</sub>

```js
// Over/Under dia por dia, con el target de cada dia y sus ajustes
```
<sub>lib/report.js:391</sub>

```js
const h = Math.max(0, (perDayHours[iso] || 0) + adjForDate(wtdAdj.map, code, iso, "hours"));
```
<sub>lib/report.js:397</sub>

```js
const s = Math.max(0, (perDaySales[iso] || 0) + adjForDate(wtdAdj.map, code, iso, "sales"));
```
<sub>lib/report.js:398</sub>

```js
if (t > 0) wOverUnder += (s / t) - h;
```
<sub>lib/report.js:399</sub>

```js
const wTrainee = Math.max(0, (wtdLabor.trainee[code] || 0) + adjTotal(wtdAdj.map, code, "trainee"));
```
<sub>lib/report.js:402</sub>

```js
const wTrainer = Math.max(0, (wtdLabor.trainer[code] || 0) + adjTotal(wtdAdj.map, code, "trainer"));
```
<sub>lib/report.js:403</sub>

```js
// PTD, con ajustes
```
<sub>lib/report.js:405</sub>

```js
const pH = Math.max(0, base.hours + (extraHours[code] || 0) + adjTotal(ptdAdj.map, code, "hours"));
```
<sub>lib/report.js:407</sub>

```js
const pS = Math.max(0, base.sales + (extraSales[code] || 0) + adjTotal(ptdAdj.map, code, "sales"));
```
<sub>lib/report.js:408</sub>

```js
const pSplh = pH > 0 ? pS / pH : 0;
```
<sub>lib/report.js:410</sub>

```js
hours: Math.round(dH),
```
<sub>lib/report.js:426</sub>

```js
sales: Math.round(dS * 100) / 100,
```
<sub>lib/report.js:427</sub>

```js
splh: Math.round(dSplh),
```
<sub>lib/report.js:429</sub>

```js
overUnder: Math.round(dOverUnder),
```
<sub>lib/report.js:430</sub>

```js
ok: dSplh >= dTarget,
```
<sub>lib/report.js:431</sub>

```js
hours: Math.round(wH),
```
<sub>lib/report.js:435</sub>

```js
sales: Math.round(wS * 100) / 100,
```
<sub>lib/report.js:436</sub>

```js
splh: Math.round(wSplh),
```
<sub>lib/report.js:437</sub>

```js
overUnder: Math.round(wOverUnder),
```
<sub>lib/report.js:438</sub>

```js
trainTotal: Math.round(wTrainee + wTrainer),
```
<sub>lib/report.js:439</sub>

```js
trainee: Math.round(wTrainee),
```
<sub>lib/report.js:440</sub>

```js
trainer: Math.round(wTrainer),
```
<sub>lib/report.js:441</sub>

```js
ok: wSplh >= dTarget,
```
<sub>lib/report.js:442</sub>

```js
hours: Math.round(pH),
```
<sub>lib/report.js:445</sub>

```js
sales: Math.round(pS * 100) / 100,
```
<sub>lib/report.js:446</sub>

```js
splh: Math.round(pSplh),
```
<sub>lib/report.js:448</sub>

```js
ok: pSplh >= pTarget,
```
<sub>lib/report.js:449</sub>

_Un dia solo cuenta como problema si falla por un margen material._

```js
// Sin esto, un dia al 99% del target se reporta igual que uno al 70%,
```
<sub>lib/report.js:493</sub>

```js
function buildVerdict(byWeekday) {
```
<sub>lib/report.js:498</sub>

```js
const totalHours = withData.reduce((s, d) => s + d.hours, 0);
```
<sub>lib/report.js:509</sub>

```js
const totalOver = withData.reduce((s, d) => s + (d.overUnder < 0 ? -d.overUnder : 0), 0);
```
<sub>lib/report.js:510</sub>

```js
const overShare = totalHours > 0 ? totalOver / totalHours : 0;
```
<sub>lib/report.js:511</sub>

```js
// Si el exceso total es trivial, la tienda esta en target. Punto.
```
<sub>lib/report.js:513</sub>

```js
detail: totalOver >= 1
```
<sub>lib/report.js:518</sub>

```js
? `Only ${Math.round(totalOver)} hours over budget across ${Math.round(totalHours).toLocaleString("en-US")} hours worked. The staffing pattern is working.`
```
<sub>lib/report.js:519</sub>

```js
: `No meaningful overage across ${Math.round(totalHours).toLocaleString("en-US")} hours worked. The staffing pattern is working.`,
```
<sub>lib/report.js:520</sub>

```js
detail: `The week runs ${Math.round(totalOver)} hours over budget, but it is spread evenly rather than concentrated on one day.`,
```
<sub>lib/report.js:533</sub>

```js
const materialOver = material.reduce((s, d) => s + (d.overUnder < 0 ? -d.overUnder : 0), 0);
```
<sub>lib/report.js:538</sub>

```js
const share = totalOver > 0 ? Math.round((materialOver / totalOver) * 100) : 0;
```
<sub>lib/report.js:539</sub>

```js
detail: `This is not a single-day problem. Across the week you are ${Math.round(totalOver)} hours over what the target allows.`,
```
<sub>lib/report.js:550</sub>

```js
export async function buildStoreTrend(storeCode, endIso, weeks = 4) {
```
<sub>lib/report.js:564</sub>

```js
const splh = h > 0 ? s / h : 0;
```
<sub>lib/report.js:612</sub>

```js
hours: Math.round(h),
```
<sub>lib/report.js:617</sub>

```js
sales: Math.round(s * 100) / 100,
```
<sub>lib/report.js:618</sub>

```js
splh: Math.round(splh),
```
<sub>lib/report.js:620</sub>

```js
ratio: target > 0 && hasData ? splh / target : null,
```
<sub>lib/report.js:621</sub>

```js
overUnder: target > 0 ? Math.round(s / target - h) : 0,
```
<sub>lib/report.js:622</sub>

```js
ok: hasData && splh >= target,
```
<sub>lib/report.js:623</sub>

```js
const tH = days.reduce((a, d) => a + d.hours, 0);
```
<sub>lib/report.js:629</sub>

```js
const tS = days.reduce((a, d) => a + d.sales, 0);
```
<sub>lib/report.js:630</sub>

```js
const tOU = days.reduce((a, d) => a + d.overUnder, 0);
```
<sub>lib/report.js:631</sub>

```js
sales: Math.round(tS * 100) / 100,
```
<sub>lib/report.js:643</sub>

```js
splh: tH > 0 ? Math.round(tS / tH) : 0,
```
<sub>lib/report.js:644</sub>

```js
h += d.hours;
```
<sub>lib/report.js:657</sub>

```js
s += d.sales;
```
<sub>lib/report.js:658</sub>

```js
ou += d.overUnder;
```
<sub>lib/report.js:659</sub>

```js
const splh = h > 0 ? s / h : 0;
```
<sub>lib/report.js:664</sub>

```js
sales: Math.round(s * 100) / 100,
```
<sub>lib/report.js:669</sub>

```js
splh: Math.round(splh),
```
<sub>lib/report.js:670</sub>

```js
ratio: h > 0 ? splh / target : null,
```
<sub>lib/report.js:672</sub>

```js
overUnder: Math.round(ou),
```
<sub>lib/report.js:673</sub>

```js
ok: h > 0 && splh >= target,
```
<sub>lib/report.js:674</sub>

```js
const allH = weekList.reduce((a, w) => a + w.totals.hours, 0);
```
<sub>lib/report.js:683</sub>

```js
const allS = weekList.reduce((a, w) => a + w.totals.sales, 0);
```
<sub>lib/report.js:684</sub>

```js
sales: Math.round(allS * 100) / 100,
```
<sub>lib/report.js:705</sub>

```js
splh: allH > 0 ? Math.round(allS / allH) : 0,
```
<sub>lib/report.js:706</sub>

_─────────────────────────────────────────────────────────────_

```js
// Tiempo de ticket por tienda: mismo esquema que buildStoreTrend
```
<sub>lib/report.js:713</sub>

_───────────────────────────────────────────────────────────── Tiempo de ticket por tienda: mismo esquema que buildStoreTrend_

```js
// pero con datos de kitchen_metrics en vez de labor/ventas.
```
<sub>lib/report.js:714</sub>

_pero con datos de kitchen_metrics en vez de labor/ventas. ─────────────────────────────────────────────────────────────_

```js
export async function buildKitchenTrend(storeCode, endIso, weeks = 4) {
```
<sub>lib/report.js:717</sub>

```js
? withData.reduce((a, d) => a + d.medianMin * d.itemCount, 0) / (wItems || 1)
```
<sub>lib/report.js:772</sub>

```js
medianMin: weightedMedian !== null ? Math.round(weightedMedian * 10) / 10 : null,
```
<sub>lib/report.js:784</sub>

```js
// Promedio por dia de la semana (mismo patron que el heatmap de SPLH)
```
<sub>lib/report.js:790</sub>

```js
weightedSum += d.medianMin * d.itemCount;
```
<sub>lib/report.js:798</sub>

```js
const median = items > 0 ? weightedSum / items : null;
```
<sub>lib/report.js:801</sub>

```js
medianMin: median !== null ? Math.round(median * 10) / 10 : null,
```
<sub>lib/report.js:808</sub>

```js
? allWithData.reduce((a, d) => a + (d.medianMin || 0) * d.itemCount, 0) /
```
<sub>lib/report.js:815</sub>

```js
medianMin: overallMedian !== null ? Math.round(overallMedian * 10) / 10 : null,
```
<sub>lib/report.js:829</sub>

_─────────────────────────────────────────────────────────────_

```js
// Tiempo de ticket de TODAS las tiendas para un periodo, con la
```
<sub>lib/report.js:838</sub>

```js
// Multiplos de la mediana de la empresa
```
<sub>lib/report.js:845</sub>

```js
// Arriba de esto, el equipo no esta cerrando tickets y la mediana no sirve
```
<sub>lib/report.js:850</sub>

```js
export async function buildKitchenWeek(isoDate) {
```
<sub>lib/report.js:853</sub>

```js
// Agrega por tienda, ponderando la mediana por volumen de items
```
<sub>lib/report.js:875</sub>

```js
agg[c].weighted += r.median_minutes * r.item_count;
```
<sub>lib/report.js:882</sub>

```js
const median = items > 0 ? a.weighted / items : null;
```
<sub>lib/report.js:893</sub>

```js
medianMin: median !== null ? Math.round(median * 10) / 10 : null,
```
<sub>lib/report.js:903</sub>

```js
if (median !== null && items >= MIN_ITEMS_TO_JUDGE) withMedian.push(median);
```
<sub>lib/report.js:909</sub>

```js
// Mediana de la empresa como referencia
```
<sub>lib/report.js:912</sub>

_Mediana de la empresa como referencia_

```js
withMedian.sort((a, b) => a - b);
```
<sub>lib/report.js:913</sub>

```js
? withMedian[Math.floor(withMedian.length / 2)]
```
<sub>lib/report.js:915</sub>

```js
if (companyMedian === null || companyMedian <= 0) {
```
<sub>lib/report.js:935</sub>

```js
s.ratio = Math.round((s.medianMin / companyMedian) * 100) / 100;
```
<sub>lib/report.js:940</sub>

```js
s.serviceNote = `${s.medianMin} min median, ${s.ratio}x the company median of ${Math.round(companyMedian * 10) / 10} min`;
```
<sub>lib/report.js:950</sub>

```js
companyMedianMin: companyMedian !== null ? Math.round(companyMedian * 10) / 10 : null,
```
<sub>lib/report.js:963</sub>

---

## `lib/require-session.js`

_const { session, deny } = await requireSession(); if (deny) return deny; /_

```js
export async function requireSession() {
```
<sub>lib/require-session.js:15</sub>

_say which stores you may read. Pass the grp of the store being requested, not the store code. /_

```js
export function requireGrp(session, grp) {
```
<sub>lib/require-session.js:30</sub>

_Reshapes a session into the plain object permissions.js expects, so the session shape and the permission logic can change independently. /_

```js
export function accessFromSession(session) {
```
<sub>lib/require-session.js:44</sub>

```js
function json(body, status) {
```
<sub>lib/require-session.js:52</sub>

---

## `lib/scale.js`

_No component defines a threshold and no component writes a hex. If you find yourself typing "#c6efce" or "105" inside a component, the answer is here._

```js
// Thresholds live in the metric_targets table in Postgres. Colors live in the
```
<sub>lib/scale.js:7</sub>

_yourself typing "#c6efce" or "105" inside a component, the answer is here.  Thresholds live in the metric_targets table in Postgres. Colors live in the_

```js
// --band-* tokens in globals.css, which are the same values the WeekView table
```
<sub>lib/scale.js:8</sub>

_--band-* tokens in globals.css, which are the same values the WeekView table has always used, so every report reads like the daily email._

```js
// This file only knows how to turn a value plus a config into a band, and what
```
<sub>lib/scale.js:11</sub>

_has always used, so every report reads like the daily email.  This file only knows how to turn a value plus a config into a band, and what_

```js
// a band looks like.
```
<sub>lib/scale.js:12</sub>

_ Amber is deliberately absent. The chain reads red or green, never orange. /_

```js
export const BANDS = {
```
<sub>lib/scale.js:24</sub>

```js
fill: "var(--band-green)",
```
<sub>lib/scale.js:26</sub>

```js
ink: "var(--band-green)",
```
<sub>lib/scale.js:28</sub>

```js
fill: "var(--band-green-soft)",
```
<sub>lib/scale.js:32</sub>

```js
text: "var(--band-green)",
```
<sub>lib/scale.js:33</sub>

```js
ink: "var(--band-green)",
```
<sub>lib/scale.js:34</sub>

```js
fill: "var(--band-red-soft)",
```
<sub>lib/scale.js:38</sub>

```js
text: "var(--band-red)",
```
<sub>lib/scale.js:39</sub>

```js
ink: "var(--band-red)",
```
<sub>lib/scale.js:40</sub>

```js
fill: "var(--band-red)",
```
<sub>lib/scale.js:44</sub>

```js
ink: "var(--band-red)",
```
<sub>lib/scale.js:46</sub>

```js
export const BAND_ORDER = ["green", "lightGreen", "lightRed", "red"];
```
<sub>lib/scale.js:57</sub>

_does not name its own line, which is the point. It is mirrored in the dt_bands view in Postgres, the only place this number is duplicated. /_

```js
export const COMFORT = 0.85;
```
<sub>lib/scale.js:65</sub>

_Naming that line per metric is better than retuning COMFORT, because COMFORT is shared and moving it would silently shift Service times too. /_

```js
function greenLineOf(cfg) {
```
<sub>lib/scale.js:80</sub>

```js
return lowerIsBetter ? target * COMFORT : target / COMFORT;
```
<sub>lib/scale.js:84</sub>

_@param {boolean} [cfg.lowerIsBetter=true]  false for metrics like SPLH @returns {"green"|"lightGreen"|"lightRed"|"red"|"none"} /_

```js
export function bandFor(value, cfg) {
```
<sub>lib/scale.js:98</sub>

```js
if (v <= target) return "lightGreen";
```
<sub>lib/scale.js:108</sub>

```js
if (v >= target) return "lightGreen";
```
<sub>lib/scale.js:114</sub>

_@param {object} q  { p25, median, p75 } @param {boolean} [higherIsBetter=true] /_

```js
export function bandByQuantile(value, q, higherIsBetter = true) {
```
<sub>lib/scale.js:129</sub>

```js
if (v >= q.median) return "lightGreen";
```
<sub>lib/scale.js:135</sub>

```js
if (v <= q.median) return "lightGreen";
```
<sub>lib/scale.js:140</sub>

```js
// ---- expressing a band ----
```
<sub>lib/scale.js:145</sub>

_---- expressing a band ---- Background plus legible text, for a filled cell._

```js
export function bandStyle(value, cfg) {
```
<sub>lib/scale.js:148</sub>

_Fill only, for bars, dots and legend swatches._

```js
export function bandFill(value, cfg) {
```
<sub>lib/scale.js:154</sub>

_Text color for a number printed on a white surface._

```js
export function bandInk(value, cfg) {
```
<sub>lib/scale.js:159</sub>

```js
/** Same three, for when the band name arrived with the data instead. */
```
<sub>lib/scale.js:163</sub>

_@param {object} cfg @param {(n:number)=>string} [fmt] /_

```js
export function bandLegend(cfg, fmt = (n) => String(Math.round(n))) {
```
<sub>lib/scale.js:178</sub>

_green_value is optional. A null leaves the metric on the COMFORT margin, which is how every metric behaved before the column existed. /_

```js
export function cfgFromTarget(row) {
```
<sub>lib/scale.js:207</sub>

_---- formatting ---- mm:ss for anything measured in seconds. Shared so every tab reads alike._

```js
export function fmtSeconds(s) {
```
<sub>lib/scale.js:223</sub>

```js
/** Formatter matched to the unit on a metric_targets row. */
```
<sub>lib/scale.js:232</sub>

_Formatter matched to the unit on a metric_targets row._

```js
export function fmtForUnit(unit) {
```
<sub>lib/scale.js:233</sub>

```js
// ---- Google and Yelp reviews ----
```
<sub>lib/scale.js:238</sub>

_---- Google and Yelp reviews ----  Reviews do not use bandFor. The COMFORT margin assumes a target with room_

```js
// on either side, but review cutoffs are not a target: they are two fixed
```
<sub>lib/scale.js:241</sub>

_below 4.00  The lightRed step at 3.50 exists only so the scale still reads in four_

```js
// bands like every other metric.
```
<sub>lib/scale.js:250</sub>

_The lightRed step at 3.50 exists only so the scale still reads in four bands like every other metric._

```js
export const REVIEW_TIERS = {
```
<sub>lib/scale.js:252</sub>

```js
/** Band for a Google/Yelp average. Higher is better. */
```
<sub>lib/scale.js:258</sub>

_Band for a Google/Yelp average. Higher is better._

```js
export function bandForRating(value) {
```
<sub>lib/scale.js:259</sub>

```js
if (v >= REVIEW_TIERS.basePlus) return "green";
```
<sub>lib/scale.js:262</sub>

```js
if (v >= REVIEW_TIERS.baseOnly) return "lightGreen";
```
<sub>lib/scale.js:263</sub>

```js
if (v >= REVIEW_TIERS.concern) return "lightRed";
```
<sub>lib/scale.js:264</sub>

```js
/** Legend for the reviews column, same shape bandLegend returns. */
```
<sub>lib/scale.js:268</sub>

_Legend for the reviews column, same shape bandLegend returns._

```js
export function reviewLegend() {
```
<sub>lib/scale.js:269</sub>

---

## `lib/tattle-auth.js`

```js
function credentials() {
```
<sub>lib/tattle-auth.js:19</sub>

```js
async function post(path, body) {
```
<sub>lib/tattle-auth.js:26</sub>

```js
function tokensFrom(json, fallbackRefresh = null) {
```
<sub>lib/tattle-auth.js:38</sub>

```js
async function save(tokens, extra = {}) {
```
<sub>lib/tattle-auth.js:54</sub>

```js
function isFresh(expiresAt) {
```
<sub>lib/tattle-auth.js:61</sub>

_Cached token, then refresh, then full login._

```js
export async function getAccessToken(force = false) {
```
<sub>lib/tattle-auth.js:68</sub>

_fetch() with the bearer token attached. Retries once on a 401._

```js
export async function tattleFetch(path, options = {}) {
```
<sub>lib/tattle-auth.js:102</sub>

---

## `lib/throughput.js`

```js
// TPLH (Transacciones Por Hora de Labor) por tienda, por semana.
```
<sub>lib/throughput.js:1</sub>

_TPLH (Transacciones Por Hora de Labor) por tienda, por semana._

```js
// A diferencia de SPLH, TPLH no se infla con aumentos de precio ni con dias
```
<sub>lib/throughput.js:3</sub>

_TPLH (Transacciones Por Hora de Labor) por tienda, por semana.  A diferencia de SPLH, TPLH no se infla con aumentos de precio ni con dias_

```js
// de ticket promedio alto. Mide throughput real por persona, que es una
```
<sub>lib/throughput.js:4</sub>

_ A diferencia de SPLH, TPLH no se infla con aumentos de precio ni con dias de ticket promedio alto. Mide throughput real por persona, que es una_

```js
// comparacion mas justa entre tiendas de distinto ticket promedio.
```
<sub>lib/throughput.js:5</sub>

_comparacion mas justa entre tiendas de distinto ticket promedio.  NOTA IMPORTANTE SOBRE DUPLICACION:_

```js
// Los helpers de horas (shiftHours, localDateInTz, exclusionReason, etc.)
```
<sub>lib/throughput.js:8</sub>

```js
function addDays(iso, n) {
```
<sub>lib/throughput.js:20</sub>

```js
function isoRange(startIso, endIso) {
```
<sub>lib/throughput.js:26</sub>

```js
function dayNameFromISO(iso) {
```
<sub>lib/throughput.js:37</sub>

```js
export function mondayOf(iso) {
```
<sub>lib/throughput.js:42</sub>

_--- copias exactas de report.js ---_

```js
function localDateInTz(utcIso, tz) {
```
<sub>lib/throughput.js:51</sub>

```js
async function fetchAllRows(buildQuery, label) {
```
<sub>lib/throughput.js:62</sub>

```js
function normName(name) {
```
<sub>lib/throughput.js:77</sub>

```js
function shiftHours(row) {
```
<sub>lib/throughput.js:81</sub>

```js
async function getExcludedEmployees() {
```
<sub>lib/throughput.js:89</sub>

```js
function exclusionReason(empName, jobTitle, excludedSet) {
```
<sub>lib/throughput.js:97</sub>

```js
async function readLaborHours(startIso, endIso, excludedSet, tzByStore) {
```
<sub>lib/throughput.js:105</sub>

```js
hours[code] = (hours[code] || 0) + shiftHours(r);
```
<sub>lib/throughput.js:128</sub>

_--- fin de copias ---_

```js
export async function buildThroughput(weekStartIso) {
```
<sub>lib/throughput.js:136</sub>

```js
if (r.error) throw new Error("daily_transactions: " + r.error.message);
```
<sub>lib/throughput.js:162</sub>

```js
if (r.error) throw new Error("daily_sales: " + r.error.message);
```
<sub>lib/throughput.js:171</sub>

```js
txnByStore[r.store_code] = (txnByStore[r.store_code] || 0) + r.transaction_count;
```
<sub>lib/throughput.js:180</sub>

```js
salesByStore[r.store_code] = (salesByStore[r.store_code] || 0) + (r.gross_sales || 0);
```
<sub>lib/throughput.js:187</sub>

```js
// Target promedio de la semana, ponderado por dia (5 entre semana, 2 fin).
```
<sub>lib/throughput.js:190</sub>

_Target promedio de la semana, ponderado por dia (5 entre semana, 2 fin)._

```js
function avgTarget(store) {
```
<sub>lib/throughput.js:191</sub>

```js
days.forEach((d) => { sum += getTarget(store, dayNameFromISO(d)); });
```
<sub>lib/throughput.js:193</sub>

```js
hours: Math.round(hours * 10) / 10,
```
<sub>lib/throughput.js:209</sub>

```js
sales: Math.round(sales),
```
<sub>lib/throughput.js:211</sub>

```js
tplh: hours > 0 && hasTxnData ? Math.round((transactions / hours) * 100) / 100 : null,
```
<sub>lib/throughput.js:212</sub>

```js
splh: hours > 0 ? Math.round((sales / hours) * 100) / 100 : null,
```
<sub>lib/throughput.js:213</sub>

```js
avgTicket: transactions > 0 && hasTxnData ? Math.round((sales / transactions) * 100) / 100 : null,
```
<sub>lib/throughput.js:214</sub>

```js
target: Math.round(avgTarget(st)),
```
<sub>lib/throughput.js:215</sub>

```js
(complete.reduce((a, r) => a + r.transactions, 0) /
```
<sub>lib/throughput.js:226</sub>

```js
complete.reduce((a, r) => a + r.hours, 0)) * 100
```
<sub>lib/throughput.js:227</sub>

```js
? Math.round(((r.tplh - companyTplh) / companyTplh) * 1000) / 10
```
<sub>lib/throughput.js:234</sub>

```js
if (a.tplh === null && b.tplh === null) return a.code - b.code;
```
<sub>lib/throughput.js:239</sub>

```js
if (b.tplh === null) return -1;
```
<sub>lib/throughput.js:241</sub>

```js
return b.tplh - a.tplh;
```
<sub>lib/throughput.js:242</sub>

---

## `lib/toast-labels.js`

_── JOBS: GUID -> nombre del puesto ──_

```js
export async function getJobsMap(restaurantGuid) {
```
<sub>lib/toast-labels.js:16</sub>

```js
const res = await fetch(`${HOST}/labor/v1/jobs`, {
```
<sub>lib/toast-labels.js:23</sub>

_── EMPLOYEES: GUID -> "Apellido, Nombre" (mismo formato que usa SHIFT) ──_

```js
export async function getEmployeesMap(restaurantGuid) {
```
<sub>lib/toast-labels.js:42</sub>

```js
const res = await fetch(`${HOST}/labor/v1/employees`, {
```
<sub>lib/toast-labels.js:49</sub>

_── Traduce un array de time entries crudos de Toast a filas listas para aggregatePayroll ──_

```js
export async function translateTimeEntries(entries, restaurantGuid) {
```
<sub>lib/toast-labels.js:73</sub>

---

## `lib/toast.js`

```js
async function fetchNewToken() {
```
<sub>lib/toast.js:6</sub>

```js
export async function getToastToken() {
```
<sub>lib/toast.js:35</sub>

```js
export async function getTimeEntries({ restaurantGuid, startDate, endDate }) {
```
<sub>lib/toast.js:42</sub>

```js
const url = new URL(`${HOST}/labor/v1/timeEntries`);
```
<sub>lib/toast.js:46</sub>

---

## `lib/ui.js`

_Shared UI helpers. No React here, pure functions only._

```js
export const GROUPS = {
```
<sub>lib/ui.js:3</sub>

_Groups rows into region sections in a fixed, predictable order. Empty sections are dropped so the table never shows a header with nothing under it. /_

```js
export function sectionize(rows, { group = "All", search = "", withGroup = false } = {}) {
```
<sub>lib/ui.js:19</sub>

```js
export const pct = (n, d = 1) => (nil(n) ? "—" : (n > 0 ? "+" : "") + dec(n, d) + "%");
```
<sub>lib/ui.js:52</sub>

```js
export function addDays(iso, n) {
```
<sub>lib/ui.js:60</sub>

```js
export function shortDate(iso) {
```
<sub>lib/ui.js:66</sub>

```js
export function yesterdayISO() {
```
<sub>lib/ui.js:72</sub>

_---- stats ---- Linear-interpolated quantile. Input must be sorted ascending._

```js
export function quantile(sorted, q) {
```
<sub>lib/ui.js:81</sub>

```js
export const median = (values) => quantile([...values].sort((a, b) => a - b), 0.5);
```
<sub>lib/ui.js:90</sub>

```js
/** Position of v inside [min,max] as a 0-100 percentage, clamped. */
```
<sub>lib/ui.js:92</sub>

_Position of v inside [min,max] as a 0-100 percentage, clamped._

```js
export function railPos(v, min, max) {
```
<sub>lib/ui.js:93</sub>

---


**884 calculations found across 70 files.**
